# 실제 실행 검증 기록

## 2026-09-11: Patient A와 origin Patient B/C 수정 병합 검증

로컬 `b0635aed`에 포함된 Patient A 전체 E2E 수정(`1c840c42`)과 origin/main `806cc91e`에 포함된 Patient B/C 전체 E2E 수정(`c203351d`)을 병합했습니다. 아래 결과는 병합 작업 트리에서 새로 빌드하고 실행한 결과이며, 두 부모 커밋의 과거 성공 기록을 이번 병합의 성공으로 대체하지 않습니다.

검증 도중 외부에서 같은 두 부모를 가진 병합 커밋 `8084b875`가 생성됐습니다. 해당 커밋을 보존하고, 이후 실기에서 확인한 A 경로 보완과 검증 기록은 후속 변경으로 정리합니다.

충돌한 입력 브리지·MCP 입력 스키마·차량 이동 도우미는 양쪽 기능을 보존했습니다. A의 상호작용 선택·실행 및 핫바 선택과 B/C의 가로·세로 스크롤을 함께 지원합니다. 차량 종류와 명시적인 CT 도착 좌표는 별도 인자로 유지하며, 스냅 이후 조종권 해제를 정상 완료로 인정하는 A의 처리도 유지합니다. 반대로 좌표 목적지가 있으면 이전 스냅 기록만으로 완료하지 않습니다. 두 경우를 구분하는 회귀 테스트를 추가했습니다.

서비스 테스트는 148/148, Python 상호작용 마이그레이션 검사는 6/6, 지도 경로 검사 테스트는 5/5, TypeScript 타입 검사는 통과했습니다. 두 시나리오 JSON은 프로젝트 시나리오 스키마로 Ajv 검증을 통과했습니다. .NET 검증 CLI를 실행할 수 없는 환경이므로 CLI 통과로 기록하지 않습니다. Unity 6000.2.8f1 macOS 자동화 빌드도 성공했습니다.

Unity EditMode 검사는 197개 중 151개 통과, 46개 실패입니다. 입력 처리 16개와 상호작용 등록 11개는 모두 통과했습니다. 실패는 A 검사 20개, B/C 검사 23개, 의식 확인 동시성 검사 3개입니다. 실패 출력에는 이전 그래프·소스 문구를 기대하는 검사, 테스트 오브젝트 초기화 오류, 모니터 신호·역할 기대값 불일치가 포함됩니다. 각 부모 커밋에서 같은 검사를 재실행한 기준선은 없으므로 이를 모두 기존 실패라고 단정하지 않으며, 전체 네이티브 회귀 검증 완료로 취급하지 않습니다. 증거는 `/private/tmp/ktas-merge-unity-tests.xml`과 `/private/tmp/ktas-merge-unity-tests.log`입니다.

실제 A 재생에서 B/C 수정으로 옮겨진 카트와 기존 A의 복귀 경로가 충돌했습니다. 기존 원거리 활력징후 세트로 향하는 경로에도 연결되지 않은 복도 구간이 있어, 하차 구역의 동일 아이템을 획득하고 갱신된 `positions.md`의 서쪽 세로 복도와 처치실 출입구를 통과하도록 배치를 수정했습니다. 장비 배치를 되돌리지 않았습니다. 실패 실행 `902d8a01-d315-49de-8319-c69d45bd2052`, `55e44d82-1133-4a19-a1b9-1f7b93e65b22`, `6c43982d-85ec-4667-80d4-6433b0ee95c8`, `4d42a979-f261-44a4-aed3-c98a60d91fe6`은 원인 확인용으로 보존합니다.

후속 실행 `c6d1b0fa-eb35-45a3-b38b-20a28f047c2d`에서 초기 사정·기관삽관은 통과했지만, 후속 장비 준비 중 P1의 동쪽 우회선이 같은 카트에 막혔습니다. 후속 복귀와 CPR 물품 왕복 경로에서도 해당 구간을 제거했습니다. 좁은 복도 통과만 순서대로 처리하고, 아이템 준비·처치 등 독립적인 행동은 병렬 실행합니다. `positions.md`의 잘못 연결된 예제 경로도 수정했습니다. 실제 전체 재검증 결과는 확인 후 아래에 추가합니다.

실행 `ed92bd13-0c64-475e-aef2-8a3874e958e6`은 장비 준비와 P4의 정맥로 확보까지 통과했습니다. P1은 산소 장비 선반 남쪽에서 획득을 마친 뒤 처치실로 직진하다 `[-65.469, -12.846]`에서 막혔습니다. 장비 획득 후 위치를 기록하고, 남쪽 면에 있으면 `z=-15.5`로 나온 뒤 `x=-68.5` 복도로 진입하도록 경로를 보완했습니다. 이 실행 역시 전체 성공으로 처리하지 않습니다.

### Patient A 최종 재생

`node test/live-patient-a-critical.ts config.json patient_a_critical full mac_direct` 실행 `ac339216-8bfd-48ed-9c8b-ce92eff49b01`은 종료 코드 0으로 통과했습니다. 초기 사정·기관삽관·병렬 산소/출혈/정맥로 처치·중심정맥관·Level 1 수액·CPR 1차와 2차·ROSC 후속 처치를 완료했습니다. `patient-a-full-route-completed.json`에서 네 클라이언트 모두 `E038`, `recoveryNotes:[]`, `fullPlayPassed:true`를 확인했습니다. 역할별 퀘스트 증거 수집은 4개 모두 fulfilled이며 프로세스는 모두 EXITED입니다.

A의 `route-manifest.json`은 시작 시 생성된 출처 기록이며 기존 스크립트가 최종 판정을 다시 쓰지 않아 `fullPlayPassed:false`가 남아 있습니다. A의 최종 성공 판정은 `patient-a-full-route-completed.json`과 종료 코드 0을 사용합니다. 시작 기록을 사후에 성공으로 덮어쓰지는 않았습니다.

이 실행에서 산소 장비 획득 후 P1 위치는 선반 북쪽 `[-64.607, -10.889]`였습니다. 따라서 추가한 남쪽 면 복귀 분기가 이 최종 실행에서도 사용됐다고 주장하지 않습니다. 최종 실행은 수정한 배치 전체 경로의 1회 통과이며, 모든 위치 변형이나 반복 안정성을 보장하는 결과가 아닙니다. 성공 기록의 배치 SHA-256은 현재 파일과 일치합니다.

### Patient B/C 최종 재생

동일한 Unity 빌드에서 `node test/live-patient-b-c-ct.ts config.json patient_b_c_ct b-care mac_direct` 실행 `de1f25ff-50d5-4b9c-94aa-5bbf931de566`도 종료 코드 0으로 통과했습니다. 네 역할의 분류·침대 이동, B/C 의식·근력·활력징후 사정, 양쪽 환자의 동공/정맥로/산소/출혈 처치와 CT 이송을 완료했습니다. 역할별 퀘스트 증거에 네 플레이어 모두 `Quest_Transport_BC_To_CT` 완료가 있으며, `ct-arrived-bed_b.json`과 `ct-arrived-bed_c.json`에 두 환자의 도착 관측을 저장했습니다. 프로세스는 모두 EXITED이며 종료 정리 오류는 없습니다.

`route-manifest.json`은 `fullPlayPassed:true`입니다. `full-play-completed.json`의 호스트 관측에는 두 환자의 CT 도착 신호, 해당 시나리오의 남은 실제 퀘스트 없음, `recoveryNotes:[]`가 있습니다. 단, 기존 B/C 배치는 이 조건을 만족하면 종료하므로 마지막 관측 노드는 `CT_FINAL_DELAY`입니다. 그 뒤의 1초 지연·종료 안내·페이드가 끝난 화면까지 검증한 결과는 아닙니다. 이번 통과는 모든 처치·이송 퀘스트 경로의 통과로 기록하며, 종료 연출 완료 또는 반복 안정성까지 확대하지 않습니다.

---

2026년 9월 8일 기준으로, macOS의 Unity 6000.2.8f1 빌드와 Node.js 26.7.0에서 확인한 결과입니다. 단위 테스트의 통과와 실제 멀티플레이 실행의 통과를 구분합니다. 결과 파일은 저장소에서 제외된 `artifacts/unity-e2e`에 보존합니다.

현재 상태(2026-09-08): 서비스 테스트 116개와 타입 검사가 통과했습니다. 최신 빌드의 CPU 프로파일 수집 실행은 20회 연속 통과했습니다. 직전 실행은 11회 통과 후 12회차 제어권 만료로 중단됐으며, 그 원인은 아직 확정되지 않았습니다. 아래 초기 결과와 후속 실행 기록은 각각 해당 시점의 소스·빌드에 대한 증거이며, 과거 20회 통과를 최신 빌드의 통과로 취급하지 않습니다.

| 검증 | 결과 | 증거 |
|---|---|---|
| 서비스·MCP·러너 | 54개 테스트가 통과했습니다. | `npm test`, `node --test test/udp-fault-proxy.test.ts` |
| TypeScript | 타입 검사가 통과했습니다. | `npm run typecheck` |
| Unity 입력·콘텐츠 목록 | 입력·콘텐츠·역할 구성·이벤트 처리기 관측 등 8개 테스트가 통과했습니다. | `input-tests.xml` |
| 일반 빌드에서 자동화 제거 | 이전 일반 macOS 플레이어의 어셈블리 검사가 통과했습니다. 최종 소스 확정 후 재검증해야 합니다. | `Build/E2E-removal-check.app` |
| 설정 화면 진입 후 사람에게 인계 | 탐색 러너가 통과했습니다. | `ffc3d453-b914-48b0-8b5b-aa1a3f0239d0` |
| 입력 프로토콜 | 입력 충돌, 해제, 중복 명령, 이전 세대 및 임대 만료 검사가 통과했습니다. | `8992cd60-3a17-4206-8085-dcfc15a8527e` |
| 튜토리얼 이동 | 고정 JSON으로 일반 이동 → 서버 시그널 → 후속 노드 전이를 확인했습니다. | `22fe4ee9-fe9a-4312-9880-82633492aefd` |
| 4인 통합 반복 | 단일 스레드 렌더링·TTS 비활성화 설정에서 20회 연속 통과했습니다. 기본 렌더링 설정의 실패는 아래에 보존합니다. | `101860a2-73ad-4aa4-9d39-1b45e92691f2/suite.json` |
| 전용 서버 + 4인 | 접속·스폰·종료가 통과했고 서버 데이터팩의 프로필 경로를 확인했습니다. | `9882b7d5-1d53-49d3-ae73-b325d3dfd798` |
| NPC 대화 선택지 | 아직 전체 경로를 통과하지 못했습니다. NPC 중복 관측을 수정한 뒤, 준비 위치에서 추락한 플레이어의 높이 불일치를 검출했습니다. | `faeb10a5-0609-4806-a248-749177a335fc` |

6회차 접속 실패의 프로세스 실행 ID는 `be089017-d198-43bc-a403-1e556a84796c`입니다. 호스트 로그에는 네 번째 플레이어가 접속한 뒤 이탈한 기록이 있습니다. 접속 준비는 `DEADLINE_EXCEEDED`로 끝났고, 실행한 프로세스의 종료 정리에는 오류가 없었습니다. 이것만으로 게임 코드, 시스템 부하 또는 네트워크 중 어느 것이 원인인지 확정할 수 없습니다. 이후 구현에서는 접속 실패 시 준비 단계와 대상 플레이어, 각 인스턴스의 상태와 화면을 추가로 저장합니다.

`game.catalogue`로 확인한 기존 15개 그래프는 모두 서버 권위 실행에서 미지원 노드를 포함합니다. `tutorial`은 `ScenarioInteractionVisibilityNode`, `disaster_intro`는 `ScenarioParallelNode` 때문에 호환 실행으로 전환합니다. 튜토리얼 E2E는 `Local` 모드를 명시적으로 검사하므로 서버 단일 권위 실행이나 역할별 동기화를 검증한 결과가 아닙니다.

기본 렌더링 설정에서의 20회 연속 통과, 60분 지속 실행, 역할별 대화·선택지·개인 체크리스트의 전체 경로, Windows 및 혼합 플랫폼 실행, 원격 게이트웨이는 아직 검증 완료 항목이 아닙니다. 실패한 실행은 보존하며, 수정 이후 검증은 새로운 실행 ID로 기록합니다.

NPC 접근 실패 화면은 `faeb10a5-0609-4806-a248-749177a335fc/npc-failure.jpg`에 있습니다. 목표 NPC 위치는 `[76.43, 0, 21.6]`이고 실패 당시 플레이어는 `[76.43, -127.906, 20.404]`였습니다. 이 실행에는 기록된 위치 준비와 TTS 비활성화 설정이 적용됐습니다. 기존 콘텐츠 배치와 준비 위치의 적합성을 추가 확인해야 하며, 정상 이동만으로 해당 위치에 도달했다는 결과가 아닙니다.

합성 대화 그래프의 첫 서버 권위 실행은 원격 플레이어의 대화 진행 단계에서 멈췄습니다. 프로세스 실행 `18bfb6bb-d471-4e26-805c-966603d4b22f`의 `input.frame` 이벤트에는 `KeypadEnter`의 눌림·해제가 기록됐지만, 같은 프레임의 `dialogueBinding.bound`는 `false`였습니다. 화면의 대화 오버레이는 활성 상태였습니다. 이를 근거로, 플레이어가 UI보다 먼저 생성된 경우 활성 대화 오버레이를 입력 코드에서 연결하도록 수정했습니다. 수정 후 검증 결과는 별도로 기록합니다.

대화 UI 연결 수정 후 합성 그래프 E2E가 통과했습니다. 실행 ID는 `a7db4d01-52af-45a9-b1f4-63baa6e40e71`이며, 원격 플레이어의 대화 진행과 두 번째 선택지 입력 뒤 서버와 해당 클라이언트가 모두 `accepted` 노드에 도달했습니다. 프로세스 실행 `1082c813-bdcf-4281-a907-5938ccf5e11e`의 종료 정리 오류는 없었습니다.

인벤토리·퀘스트 UI 격리 검증도 프로세스 실행 `866820da-7a3d-4b31-ac2b-67ac0178aaf0`에서 통과했습니다. P2에서 두 UI를 각각 열고 닫는 동안 호스트의 같은 UI는 숨김 상태로 유지됐습니다. 이 두 실행은 TTS를 비활성화한 프로필을 사용했습니다.

통신 입출력을 Unity 동기화 컨텍스트에서 분리한 빌드의 입력 프로토콜 검증은 `4bfbc460-11c5-4d1a-83f1-ac2c2541e1c2`에서 통과했습니다. 이 수정 전 통합 반복 실행 `0bb2f82a-5dcc-418c-ab84-57b57efc40e2`는 1회 통과 후 2회차 제어권 만료로 중단됐습니다. 새 반복 실행에서는 `resources.json`에 명령 지연, 러너 이벤트 루프 지연과 시스템 부하를 기록합니다.

프레임률 관측을 추가한 실행 `ecc57cf5-a829-4558-8f41-5df256d21cb6`에서 네 플레이어의 실제 목표 프레임률이 모두 500FPS임을 확인했습니다. 프로필의 30FPS 설정을 FishNet이 접속 시 덮어쓰고 있었습니다. 프로젝트의 연결 준비 코드에서 FishNet 공개 API로 자동 프레임 설정을 해제하도록 수정했으며, 수정 후 실제 적용값과 반복 안정성은 별도 실행으로 검증합니다. 이때 Unity 작업 스레드는 실행 옵션 `-job-worker-count 2`로 제한했고 관측값도 2였습니다.

30FPS 유지 수정 후 통합 반복 실행 `d39e1cd3-515a-402e-9add-73ed7463727f`는 8회 연속 통과한 뒤 9회차 접속 준비에서 중단됐습니다. 실패 프로세스 실행 `11374c04-9843-4c60-857f-be39d1f3c00d`에서는 P3 브리지가 응답하지 않았고 호스트가 P3를 연결 시간 초과로 내보냈습니다. 다른 인스턴스의 목표 프레임률은 30FPS였으며 종료 정리 오류는 없었습니다. 로그에 원인을 확정할 예외가 없어, 이후 동일 장애에서는 관리자가 직접 실행한 macOS 플레이어의 스레드 스택도 수집하도록 진단을 추가했습니다.

지속 실행 명령의 1분 검증은 `d3e39f40-4b4d-4243-a93a-b4bb1f779481`에서 통과했습니다. 이 결과는 입력 없이 네 플레이어의 준비 상태와 프레임·서버 틱 진행을 주기적으로 확인한 결과이며, 60분 검증 완료를 뜻하지 않습니다.

진단을 추가한 다음 실행 `0930cb19-6835-45ad-a2bb-cbb9f81e5353`에서도 P4 접속 중 응답 정지가 발생했습니다. `process-sample-0930cb19-6835-45ad-a2bb-cbb9f81e5353-p4.txt`의 모든 메인 스레드 표본이 UI Toolkit RepaintPanels → Metal BeginBufferWriteThreadSafe → BufferMetal::BeginWrite → semaphore_wait_trap 경로에 있었습니다. 렌더 스레드는 ThreadedStreamBuffer에서 명령 입력을 기다리고 있었습니다. 원인이 확정된 것은 아니지만 렌더링 정지를 조사할 근거를 확보했으며, 단일 스레드 렌더링 옵션을 적용하는 `mac_direct` 비교 설정을 별도로 추가했습니다. 기존 기본 설정의 실패를 이 설정으로 성공 처리하지 않습니다.

로컬 호환 실행을 클라이언트로 표시하도록 수정한 뒤, `mac_direct` 튜토리얼 실행 `22fe4ee9-fe9a-4312-9880-82633492aefd`가 통과했습니다. 프로세스 실행 `81425eb8-0f76-4948-ae2b-9b31460f487a`의 네 Unity 로그에서 `kGfxThreadingModeDirect` 적용을 확인했고 종료 정리 오류는 없었습니다. 이 단일 실행만으로 렌더링 정지가 해결됐다고 판단하지 않습니다.

단일 스레드 설정의 첫 통합 반복 `3aa88c73-56bb-41bf-9957-d1d88626944f`는 첫 회차 P3의 타이틀 UI 전환에서 실패했습니다. 실행 `755c4394-928f-4fb8-817c-559a744ae5d5`에서는 프레임과 브리지 응답이 정상이고 첫 `ui.activate`도 완료됐지만 타이틀 화면이 유지됐습니다. EventSystem 소스에서 입력 모듈이 바뀐 프레임에는 Process를 호출하지 않는 것을 확인해, 가상 입력 모듈과 포인터를 먼저 준비한 뒤 다음 프레임에 클릭하도록 수정했습니다. 클릭 자동 재시도는 추가하지 않았습니다.

역할 분기용 합성 그래프와 `role-branches-fixture.e2e.json`, 서버 분기 배정표·플레이어 인벤토리·체크리스트 완료 항목 관측을 추가했습니다. 역할별 실행의 실제 빌드 검증은 아래 후속 실행 기록에 정리했습니다. 개인 체크리스트의 실제 획득 경로는 아직 검증하지 않았습니다. 체크리스트가 없거나 인벤토리를 관측할 수 없을 때 미완료 판정이 성공하지 않는 러너 테스트는 통과했습니다.

통합 반복 실행 `101860a2-73ad-4aa4-9d39-1b45e92691f2`가 20회 모두 통과했습니다. 매 회차 독립 프로세스 4개를 새로 시작해 접속·UI 격리·서버 권위 대화 및 선택지 전이·종료 정리를 검사했으며 정리 오류는 없었습니다. 설정은 `mac_direct`, 960×540, 목표 30FPS, 작업 스레드 2개, TTS 비활성화였습니다. 첫 클릭 준비 수정까지 포함한 빌드의 결과이며, 이후 추가한 역할 그래프와 인벤토리 관측은 포함하지 않습니다. 기본 멀티스레드 렌더링의 실패나 전체 P0 완료를 이 결과로 대체하지 않습니다.

역할 그래프의 첫 실행 `4b7c0ca7-6931-4b8b-91f6-976517b15b8e`는 시작 전 역할 태그가 없어서 콘텐츠 실행기에 의해 거부됐습니다. 프로세스 실행 `e6d3f544-f7bf-4ff1-8b6b-af540c7d1ad9`의 호스트 채팅 로그에 거부 사유가 있습니다. 기존 시작 규칙에 맞춰 역할 태그 배정을 명시적인 준비 단계로 분리했으며, 이후 서버 배정·분기 선택·합류 검증은 유지합니다.

역할 준비 후 실행 `3ff83a95-c633-4ed3-8980-38adaa724d36`는 서버의 4인 분기 배정, 각 화면의 서로 다른 선택지, 역할 태그 격리까지 통과했습니다. 이후 분기 완료 상태 대기에서 실패했습니다. 합성 그래프가 완료 상태 기록 노드와 분기 종료 표식에 같은 식별자를 사용해, `RunBranchChain`이 상태 기록 직전에 종료하는 것이 원인이었습니다. 종료 표식을 별도 식별자로 바꾸고, 선택지의 다음 노드가 종료 표식과 같지 않아야 한다는 네이티브 검증을 추가했습니다. 수정 전 실패 기록과 프로세스 실행 `948c5185-a406-4eeb-9456-546c470075f7`은 보존합니다.

종료 표식 수정 후 역할 분기 실행 `e68470c9-7e4f-4bd2-9b10-9fd31963cc14`가 통과했습니다(반복 묶음 `38076dbe-d68d-4cda-8115-50ee6f1c9735`, 프로세스 실행 `574a473b-b8ff-4c4e-8d18-2526d8179ff7`, `mac_direct`, 1회). 서버의 정확한 4인 배정, 16개 역할 태그 검사, 화면별 선택지, 먼저 완료한 3명의 상태 기록과 합류 대기, 마지막 플레이어 완료 후 4개 화면의 합류 노드 진입을 확인했습니다. 정리 오류는 없었습니다. 역할 태그는 명시적인 채팅 명령 준비 단계에서 부여했고, 이후 선택은 정상 입력으로 실행했습니다. 실행 모드는 `Local` 호환 모드이며 서버 단일 권위 시나리오 실행의 증거로 사용하지 않습니다. 네이티브 테스트도 2026-09-07 20:22:32 UTC에 7개 모두 통과했습니다.

개인 체크리스트의 실제 획득 경로를 준비하기 위해 `game.observe.worldItems`와 아이템 종류별 `navigate` 대상을 추가했습니다. 대상이 중복되거나 사라졌을 때 성공하지 않는 검증을 포함해 Node 테스트 32개, TypeScript 타입 검사, 자동화 심볼을 적용한 C# 컴파일이 통과했습니다. 이 추가분은 아직 Unity 빌드 재생성 및 실제 아이템 획득 검증을 거치지 않았습니다.

실제 아이템 버리기 실행 `faec2657-68b0-4864-9d7b-868f55975a7c`에서 즉시 재획득 문제가 재현됐습니다. `after-drop-samples.json`의 569프레임에는 혈액백이 바닥에 있고 호스트 인벤토리가 비어 있으나, 같은 위치의 577프레임에는 혈액백이 다시 인벤토리로 들어왔습니다. 버리기 오프셋(전방 1.25m, 위 0.35m)이 자동 획득 반경 1.3m 안에 있어 발생했습니다. 전방 거리를 1.5m로 수정했으며, 실제 빌드 재검증은 아직 진행하지 않았습니다.

체크리스트 준비 실행 `da24577d-f67d-4c6c-b399-c34c3daa91fc`는 `/give` 명령이 단일 대상만 허용하므로 `@a` 지급을 거부한 뒤 종이 보유 검사에서 실패했습니다. 역할 분기 준비 실행 `1d011d34-0ba9-4463-a12a-f21cbfb72a9e`는 통과했습니다. 지급 준비를 플레이어별 `fish:0`~`fish:3` 명령으로 수정했으며 실패 기록은 보존합니다.

단일 대상 지급으로 재실행한 `6dbd84f3-cd4c-43ea-af49-ebdf60fc6a87`에서는 서버 채팅에 네 명 모두 지급 성공으로 기록됐지만, 호스트를 제외한 세 플레이어의 소유자 인벤토리는 비어 있었습니다. `/give`가 서버 인스턴스의 인벤토리만 수정하는 경로를 확인했습니다. 원격 소유자에게 TargetRpc로 지급을 전달하도록 수정했으며, 응답 문구는 전달 요청 사실만 알리도록 했습니다. 실제 수정본 검증은 아직 남아 있습니다.

실제 체크리스트 획득 실행 `79bea839-ecdb-485e-a39f-f7be455e8aa4`가 통과했습니다(`mac_direct`, 호스트+클라이언트 3명, 종료 코드 0). 역할 분기 준비 실행은 `674f626c-7d75-44f5-81de-6e92f27f4865`입니다. 플레이어별 명령 지급으로 네 개의 체크리스트를 준비한 뒤, 호스트의 정상 Q 입력으로 혈액백을 버렸습니다. 정지 상태에서도 혈액백이 바닥에 남는 것을 확인했고, P2가 이동 입력으로 접근해 획득했습니다. `p1-checklist-result.json`~`p4-checklist-result.json`에서 P2만 혈액백 1개 및 완료 항목 `blood_bag`를 보유하고 나머지는 혈액백 0개·완료 항목 없음으로 검증했습니다. 체크리스트와 초기 혈액백 지급은 준비 명령이며, 실제 획득·완료 처리는 정상 입력 경로입니다. 이 검증은 역할 분기의 Local 호환 모드에서 수행했습니다. 기존 NPC 대화부터 시작하는 전체 콘텐츠 경로를 검증한 결과는 아닙니다. Unity 네이티브 테스트도 2026-09-07 20:38:57 UTC에 7개 모두 통과했습니다.

Chrome 웹 콘솔에서 실행 `0658684a-e1c4-45ac-8691-8cd02289585d`를 생성하고 `mac_direct` 호스트+클라이언트 3명의 접속 준비를 완료했습니다. 실제 화면에서 왼쪽 게임 화면·네 개 썸네일과 오른쪽 상태·로그를 확인했습니다. P2를 선택해 RemoteHuman 제어권을 받고 브라우저 E 키로 인벤토리를 열고 닫은 뒤, AI 반환 버튼으로 `Automation` 전환을 확인했습니다. 콘솔의 선택 프로세스 종료 버튼으로 P2가 EXITED가 되는 것을 확인하고 나머지 프로세스도 종료했습니다(`console-cleanup.json`에 네 개 EXITED). 인증이 없는 새 탭에서 발생하던 JSON 구문 오류는 명확한 인증 안내로 수정해 실제 화면에서 확인했습니다. 종료 세션 선택 시 낡은 화면이 남지 않도록 선택과 관측 표시도 정리했으며, 종료 후 화면에서 실행 중인 인스턴스가 없다는 안내를 확인했습니다. 임시 키 진단 로그는 제거했습니다. 카메라 포인터 잠금과 마우스 클릭 제어의 최신 4인 콘솔 검증은 아직 남아 있습니다.

수동 기록 초안 생성을 추가했습니다. UI 조작의 단계 변환, 상태 대기, 키 유지 갱신 중복 제거, 불확실한 입력·이벤트 누락의 실행 차단을 포함해 Node 테스트 35개가 통과했습니다. 실제 브라우저 기록을 새 서비스 버전에서 생성·재생하는 검증은 아직 남아 있습니다. 최신 Unity 빌드의 60분 idle-room 검증은 실행 `1dbc131c-7736-48d2-ab63-5f63c36e9bf6`에서 진행 중이며 아직 합격 결과가 아닙니다.

기록 중 씬·입력 문맥 관측을 보존하고 초안에 상태 대기로 연결했습니다. 키 입력보다 결과 상태 대기가 앞서지 않는지, 다른 그래프의 동명 노드를 거부하는지 등을 포함해 Node 테스트 37개와 타입 검사가 통과했습니다. 실제 브라우저 기록의 생성·재생 검증은 여전히 남아 있습니다. 60분 안정성 검증은 같은 실행에서 계속 진행 중입니다.

기록 종료 시 연결이 끊겨도 원시 기록을 보존하고 실행 초안을 차단하는 테스트, 파일 저장 실패 후 같은 기록을 다시 저장하는 테스트를 추가했습니다. 전체 실행 중 기존 제어권 인계 테스트가 고정 20ms 대기 후 아직 획득되지 않은 제어권을 넘기려다 실패했습니다. 실제 획득 완료를 기다리도록 테스트를 수정한 뒤 39개가 모두 통과했습니다. 제품의 제어권 검사 조건은 완화하지 않았습니다.

요구사항별 구현 범위와 검증의 한계는 [COVERAGE.md](COVERAGE.md)에 정리했습니다. 전체 목표와 출시 게이트는 아직 충족하지 않았습니다.

NPC 접근 실패 진단을 위해 NPC 관측에 아래 방향 충돌면 샘플 `groundProbe`를 추가했습니다. 자동화 심볼을 적용한 C# 컴파일은 통과했습니다. 현재 60분 검증이 사용하는 빌드는 교체하지 않았으며, 새 관측의 실제 Unity 빌드 검증은 해당 실행이 끝난 뒤 진행합니다.

게임 통신의 장애 주입을 위해 UDP 전용 로컬 프록시를 추가했습니다. 실제 UDP 에코 통신에서 전달, 양방향 지연, 100% 손실, 설정 만료 후 회복, 종료 시 대기 패킷 정리를 확인했습니다. 전체 Node 테스트 42개가 통과했습니다. 프록시는 아직 Unity 실행 관리·MCP 도구에 연결하지 않았으며, 실제 Tugboat 장애 검증은 남아 있습니다.

UDP 프록시를 실행 관리와 `network.fault`·`network.status` MCP 도구에 연결했습니다. 테스트용 자식 프로세스로 호스트 포트 유지, 클라이언트별 프록시 포트 전달, 대상별 정책 격리, 호스트 적용 거부, 장애 기록, 종료 후 소켓 정리를 확인했습니다. 전체 Node 테스트 43개와 타입 검사가 통과했습니다. 실제 Unity/Tugboat 실행의 장애 검증은 아직 하지 않았으며, 진행 중인 60분 실행에도 프록시를 소급 적용하지 않았습니다.

`test/live-network-fault.ts`에 실제 Unity UDP 차단·회복 검증 절차를 추가하고 타입 검사를 통과했습니다. 진행 중인 60분 실행과 부하가 겹치지 않도록 아직 실행하지 않았습니다. 스크립트 작성 자체를 Tugboat 장애 검증의 통과로 보지 않습니다.

UDP 경계 테스트를 보강한 뒤 6개 모두 통과했습니다. 기존 송신자를 유지한 채 다른 포트의 송신자를 거부하고, 1024바이트/초 정책으로 전달 간격을 제한하며, 5초 초과 대기 및 1MiB 초과 대기열을 거부하는지 실제 localhost 소켓으로 확인했습니다. 최초 테스트의 60,000바이트 패킷은 수신되지 않아 기한 초과로 실패했습니다. 테스트 패킷을 8192바이트로 수정하고 송신 오류를 즉시 노출하도록 했으며, 제한 조건은 변경하지 않았습니다. 이 결과는 Unity 게임 통신의 장애 검증을 대신하지 않습니다.

콘솔에 수동 기록 검토 영역을 추가했습니다. 기록 종료 결과의 실행 ID와 파일 이름으로 초안을 읽고, 변환 차단 사유가 없고 definition이 있는 경우에만 편집기로 가져올 수 있습니다. 가져오기는 자동 실행하지 않습니다. Chrome에서 초기 안내와 비활성 가져오기 버튼을 확인했습니다. 전체 Node 테스트 46개, 타입 검사, console.js 문법 검사가 통과했습니다. 현재 실행 중인 서비스는 이전 백엔드 버전이므로 실제 기록 종료부터 가져오기까지의 브라우저 검증은 서비스 갱신 후 수행해야 합니다.

수동 콘솔에서 누락된 숫자 슬롯 키, 좌우 보조 키, 휠, 포인터 잠금 중 마우스 버튼 입력을 연결했습니다. 잠금 해제 시 입력을 정리합니다. 실제 console.js를 실행하는 독립 테스트에서 Alpha2 누름·해제, 휠 방향, 우클릭의 Mouse1 변환, 같은 인스턴스·epoch로 입력 해제, 텍스트 편집 중 입력 차단을 확인했습니다. 이 테스트는 브라우저 이벤트와 게임의 실제 반응까지 검증한 결과는 아닙니다.

역할 경계 검증을 위한 고정 E2E 두 개를 추가했습니다. role-absent-fixture는 네 플레이어가 연결된 상태에서 네 번째 역할을 부여하지 않고 서버 배정 [0,1,2,-1]과 나머지 세 분기 완료 후 합류를 요구합니다. role-multiple-tags-fixture는 P3에 두 역할을 부여하고 서버 배정 [0,1,2,2], 첫 분기 완료 후 두 번째 선택지, 두 역할 완료 후 합류를 요구합니다. 두 정의의 정적 검증은 통과했지만 실제 Unity 실행은 아직 하지 않았습니다. 부재 역할용 새 그래프는 다음 빌드에 포함해야 합니다.

관찰용 `E2E_OBSERVER_TOKEN`과 도구 허용 목록을 추가했습니다. 자격 검증·도구 분류 테스트 2개와 실제 자식 HTTP 서비스 테스트 1개가 통과했습니다. 관찰 토큰으로 조회는 가능하지만 입력·프로세스 실행·테스트 실행·장애 주입은 403으로 거부되며, 요청의 caller를 Automation으로 바꾸어도 권한을 높일 수 없습니다. 인증 링크는 0600 파일로 저장하고 서비스 로그에서 두 토큰이 출력되지 않는지 확인했습니다. 일반 조작과 준비 명령의 세부 분리 및 HTTPS 게이트웨이는 아직 남아 있습니다.

전체 서비스 테스트 50개가 함께 통과한 뒤 선택형 HTTPS 수신을 추가했습니다. `gateway` 설정으로 인증서·키·공개 Origin·바인딩 주소를 지정하며 기본 localhost HTTP 동작은 유지합니다. 새 HTTPS 테스트와 기존 HTTP 권한 테스트가 모두 통과했습니다. 임시 인증서를 신뢰 목록에 추가한 클라이언트가 정상 연결했고, 잘못된 Origin·토큰 및 관찰 토큰의 조작은 거부됐습니다. 실제 원격 PC·도메인 연결과 세부 준비 명령 권한 분리는 아직 남아 있습니다. 운영 중인 콘솔은 HTTPS 추가 이전 코드로 실행 중이며 localhost 설정입니다.

공식 MCP 클라이언트가 HTTPS 서비스에 연결하는 검증도 통과했습니다. MCP 자식 프로세스는 `NODE_EXTRA_CA_CERTS`로 임시 CA를 신뢰했고, 관찰 토큰으로 인스턴스 조회가 성공하며 입력 해제는 PERMISSION_DENIED로 반환됐습니다. 서비스 URL 검증 테스트도 통과했습니다. 이 결과는 로컬 TLS 종단 검증이며 실제 원격 장비 접속을 증명하지 않습니다.

60분 실행 중 53.1분까지 저장된 45개 청크·2700개 표본을 분석해 `soak-interim-analysis.json`에 보존했습니다. 표본 간 프레임 정지·역행은 없었지만 전체 평균 프레임률은 플레이어별 약 15.5~16.0FPS로 목표 설정 30FPS에 미달했습니다. 최대 관측 간격은 약 6초였습니다. 이는 중간 결과이며 60분 최종 판정을 대신하지 않습니다. 프레임 진행·연결 유지가 통과하더라도 30FPS 성능 목표를 충족한 것으로 해석하지 않습니다.

HTTPS·MCP·관찰 권한·콘솔 입력·UDP 프록시 변경을 함께 실행한 전체 테스트 52개가 통과했습니다(`/tmp/e2e-tls-mcp-integrated-tests.log`).

60분 idle-room 실행 `1dbc131c-7736-48d2-ab63-5f63c36e9bf6`이 종료 코드 0, state=passed로 완료됐습니다. `soak-report.json`에서 cleanupErrors가 비어 있고 네 프로세스 모두 EXITED임을 확인했습니다. 중간 성능 분석에서 평균 30FPS 목표 미달을 확인했으므로 이 결과는 지속 연결·프레임·서버 틱 진행의 검증으로 한정합니다. 이후 실제 UDP 장애 테스트를 별도 실행했습니다.

실제 Unity/Tugboat 장애 실행 `b9fec23d-0f0c-40dd-b6c9-146bc25a1550`이 통과했습니다. P2의 UDP를 2초 동안 차단했으며 장애 적용 중 40개, 전체 장애 구간에서 58개 패킷을 버렸습니다. 차단이 약 1초 남은 시점에도 입력 후 관측이 완료됐습니다. 정책 만료 후 정상 이동과 호스트 위치 재동기화, 연결 수 4 유지, P3/P4의 장애 드롭 0개를 확인했습니다. 네 프로세스 모두 EXITED이며 정리 오류가 없습니다. 지연·지터·대역폭 조합의 실제 게임 검증은 별도로 남아 있습니다.

다중 역할 첫 실행 `830a1e9d-a287-474e-90a8-e825c26c6b6a`은 분기 표시 대기에서 실패했습니다. 서버 채팅은 역할이 없는 P4까지 @a로 지정했기 때문에 시나리오 시작을 거부했다고 기록했습니다. 네 프로세스의 시나리오는 모두 Inactive였으며 역할 순차 처리 자체에 진입하지 않았습니다. 준비 단계에서 P4에 관찰자 태그를 붙이고 실행 대상에서 제외하도록 다중 역할·부재 역할 정의를 수정했습니다. P4의 방 연결은 유지하며, 역할 배정과 실제 선택지 완료 판정은 유지합니다.

대상 선택을 수정한 다중 역할 실행 `d85a4d1e-d0e8-4a8a-8029-62c40e051ef7`은 서버 배정 [0,1,2,2]와 P3의 첫 역할 완료·두 번째 노드 표시까지 진행한 뒤 TARGET_NOT_INTERACTABLE로 실패했습니다. 새 두 번째 선택지의 표시와 입력 가능 상태를 구분하지 않은 테스트 절차가 원인이었습니다. 두 번째 dialogue.hasChoices 상태 대기를 추가했으며 입력을 자동 재시도하지 않습니다. P4는 실행 대상에서 제외되어 Inactive였습니다.

입력 가능 상태 대기를 추가한 다중 역할 실행 `6f984881-480c-4dd3-88b2-27bdb84d4c34`이 통과했습니다(반복 묶음 `cf2f585b-2ebb-4106-8414-76e627bc507f`, 1회, 종료 코드 0, 정리 오류 없음). 서버가 두 역할을 P3에 배정하고 P3가 각 선택지를 순서대로 완료한 뒤 P1/P2/P3가 합류하는 것을 확인했습니다. P4는 역할 없는 관찰자로 방에 남아 게임 입력 문맥을 유지했습니다. 이 실행은 Local 호환 모드이며 서버 단일 권위 병렬 실행을 증명하지 않습니다.

2026-09-07 22:00:09 UTC에 Unity 네이티브 테스트 7개가 통과했고, 지면 관측과 부재 역할 그래프를 포함한 macOS 자동화 빌드가 성공했습니다. 새 빌드의 부재 역할 실행 `21627b94-7891-43bd-b442-170514efa770`도 통과했습니다(반복 묶음 `4dcc0c5e-d2ac-4986-8235-eb8d2a89c4cd`, 1회, 종료 코드 0, 정리 오류 없음). 서버 배정 [0,1,2,-1], 네 번째 역할의 모든 플레이어 미보유, 세 분기 완료 후 참여자 합류와 P4의 게임 문맥 유지를 확인했습니다.

NPC 지면 관측 실행 `c7f99ff9-57c3-4671-9865-ae9bb7f85111`에서 일반 멀티플레이 맵의 튜토리얼 NPC 좌표 아래 지면이 없음을 네 플레이어에서 확인했습니다. 소스 조사 결과 타이틀의 튜토리얼 버튼은 전용 TutorialScene을 열며 외부 접속을 차단합니다. 일반 맵에서 그래프만 실행한 기존 준비 방식이 잘못됐습니다. 실제 버튼으로 진입한 `1f2992b8-9191-48e4-83bc-2d7eaa979cba`에서는 NPC 아래 InvisFloor와 가까운 플레이어 스폰을 확인했습니다. 이 실행은 이미 소유한 제어권을 다시 획득하는 테스트 코드 때문에 CONTROL_DENIED로 중단됐으며 중복 요청을 제거했습니다. NPC 콘텐츠 좌표는 변경하지 않았습니다.

전용 튜토리얼 실행 `06733249-c589-4f79-aafe-95d8d02d3a51`은 정상 이동으로 NPC에 접근하고 말 걸기 입력을 적용했지만, 이동 학습 Validator에 머물러 선택지 대기가 만료됐습니다. 현재 이동 학습은 키보드·마우스를 모두 사용하고 조작 시간을 누적해야 합니다. 짧은 접근 이동만으로는 조건이 충족되지 않았습니다. 정상 키보드·카메라 조작 후 V_TUT_HAT_TALK_START 상태를 확인하도록 절차를 수정했습니다. 성공 신호를 직접 주입하지 않았습니다.

전용 튜토리얼 정상 UI 실행 `a830769a-0880-4c57-ba70-18b83964eaae`이 통과했습니다. 타이틀 btnTutorial → TutorialScene 자동 시나리오 시작 → 키보드·카메라 학습 → 대화 Validator 대기 → 정상 NPC 접근 → 말 걸기 → C_TUT_GREETING#2 선택 → D_TUT_HAT_STARTING_NO_1을 확인했습니다. 순간이동·채팅 준비 명령·직접 신호 주입을 사용하지 않았습니다. 종료 코드 0이며 프로세스는 EXITED입니다. 단독 튜토리얼 경로의 결과이고 4인 튜토리얼 검증이 아닙니다. 저장 화면에는 show_tutorial_interaction_hint 핸들러 미등록 오류가 표시되어 별도 확인이 필요합니다.

카탈로그에 InvokeEvent 노드의 eventIdentifier와 현재 핸들러 등록 상태를 추가했습니다. 핸들러 상태는 씬 초기화에 따라 달라지므로 콘텐츠 해시에 포함하지 않습니다. Unity 테스트에서 미등록→등록 상태 변화, 콘텐츠 해시 유지, 조회 중 핸들러 미실행을 확인했고 2026-09-07 22:12:12 UTC에 네이티브 테스트 8개가 모두 통과했습니다. 현재 macOS 빌드에는 이 최신 진단 추가가 아직 포함되지 않았으며, 안내 핸들러 자체의 누락도 해결된 것으로 표시하지 않습니다.

콘솔 기록 검증 준비 실행 `85db6cf0-05bb-4279-81fe-4b9cd187a393`에서 btnTutorial의 세 개별 ui.activate 요청이 완료 응답을 반환했지만 IntroScene에 남았습니다. 버튼 조회는 visible/enabled/interactable=true였습니다. 단독 러너의 정상 UI 진입 성공과 다르므로 콘솔 기록 검증은 아직 완료하지 않았습니다. 자동 회귀 재시도로 실패를 지운 것이 아니라 탐색 요청을 별도로 수행했으며 마지막 상태를 console-ui-no-transition.json에 보존했습니다. 입력 모듈·가상 장치 상태를 추가 관측해야 원인을 확정할 수 있습니다.

콘솔 수동 기록 실행 `ea2d3416-3243-4f33-b3ed-ff7f13f0531d`에서 Chrome의 실제 E 키로 인벤토리를 열고 닫았습니다. 기록 `a39f8b21-5e0f-4ccc-a5c0-309de4a296b9`는 차단 사유 없이 생성됐고, 가져오기 버튼으로 편집기에 반영됐습니다. 검토 과정에서 인벤토리 검증과 무관한 마지막 튜토리얼 노드 판정만 제외했습니다. 시작 씬과 Gameplay 상태, E 입력, InventoryUIController 상태, E 입력, Gameplay 복귀의 6단계와 관측된 키 유지 시간 60/62ms는 유지했습니다.

첫 재실행 `05b3dcb6-25b3-439b-aa2c-767f9c6eea75`는 AI 반환 후 러너가 신규 제어권 획득을 요청해 CONTROL_DENIED로 차단됐습니다. 러너 시작 시 이미 Automation 소유라면 정상 handoff로 입력을 정리하고 epoch를 변경하도록 수정했습니다. 사람 소유권은 가져오지 않습니다. 수정 후 새 프로세스 실행 `349c8a26-86a4-4ba0-a606-daf48a09a894`에서 콘솔로 시작한 `2b23575c-b6dd-4166-808a-357e4dcc278c`는 6단계 모두 통과했고 정리 오류가 없었습니다. 두 실행의 보고서를 모두 보존합니다. 전체 Node 테스트 54개와 타입 검사도 통과했습니다(`/tmp/e2e-runner-transfer-tests.log`).

새 UI 진단 빌드에서는 콘솔의 타이틀 버튼으로 TutorialScene에 진입했습니다. 이전 `85db6cf0-05bb-4279-81fe-4b9cd187a393`의 버튼 무반응 원인은 아직 확정하지 못했으며, 이번 성공으로 해결됐다고 판단하지 않습니다.

콘솔 마우스 탐색 실행 `02744668-2a8e-410c-ac3e-2e30d9a8ead1`에서 브라우저 이미지 클릭이 ui.pointer를 통해 TutorialScene 진입을 일으켰습니다. 사용한 브라우저 도구의 locator.click은 position 옵션을 지원하지 않아, 의도한 설정 버튼 좌표를 검증한 것으로 보지 않습니다. 이후 브라우저 제어 연결이 끊겼지만 페이지 heartbeat는 유지됐고, 관측한 게임 입력은 horizontal=vertical=0, W=false였습니다. 실행 관리자로 게임을 종료해 EXITED와 owner=None을 확인했습니다. 포인터 잠금·카메라의 실제 브라우저 검증은 아직 남아 있습니다.

마우스 경로 점검 중 콘솔이 UI 드래그의 중간 위치를 전송하지 않는 누락을 확인했습니다. pointermove의 최신 좌표를 50ms 단위로 모아 눌린 상태로 전송하고, pointerup 및 입력 해제 시 대기 좌표를 폐기하도록 수정했습니다. 실제 console.js를 실행하는 VM 테스트에서 누르기→중간 이동→떼기 순서, 화면 좌표 변환과 프레임 정보, 긴급 해제 뒤 대기 이동 폐기를 확인했습니다. 해당 테스트와 JavaScript 문법 검사가 통과했습니다. Unity 설정 슬라이더의 실제 드래그 검증은 아직 완료하지 않았습니다.

화면 프레임 불일치로 ui.pointer의 떼기 요청이 거부될 때 콘솔이 같은 인스턴스·epoch에 input.release_all을 보내도록 보완했습니다. 좌표 클릭을 다시 실행하지 않습니다. 실제 콘솔 이벤트 처리 테스트에 STATE_CONFLICT 응답을 주입해 해제 대상 보존을 확인했습니다. `test/live-pointer-release.ts`로 실제 macOS 빌드에서 누르기 → 미래 프레임을 넣은 떼기 요청 거부 → 좌표 없는 입력 해제 → 가상 마우스 해제를 검증했고, 실행 `54943bab-c027-4019-938c-4801fe359963`이 통과했습니다. requestedPressed와 virtualMousePressed가 모두 false였으며 프로세스가 EXITED로 정리됐습니다. 이 실행은 브리지의 해제 경로를 검증하며 실제 브라우저 슬라이더 드래그의 검증을 대신하지 않습니다. 타입 검사와 콘솔 입력 테스트도 통과했습니다.

실제 콘솔 드래그 실행 `2989b799-b495-4f1b-a746-457df4711642`에서 설정→오디오 탭에 화면 좌표 클릭으로 진입했습니다. 첫 볼륨 드래그는 값 100을 유지했고 브리지의 마지막 좌표도 시작점에 남았습니다. 게임 이미지의 기본 브라우저 드래그를 `draggable=false`와 dragstart.preventDefault로 막은 뒤 같은 게임에서 새 콘솔 코드를 불러왔습니다. 드래그 결과 볼륨이 100→50으로 바뀌고 저장 안내가 표시됐습니다. 화면 증거는 `console-volume-50.json`입니다. 기본 이미지 드래그 방지를 콘솔 이벤트 테스트에 추가했고 통과했습니다.

같은 실행에서 TutorialScene에 화면 클릭으로 진입한 뒤 카메라 조작 버튼을 눌렀지만 `WrongDocumentError: The root document of this element is not valid for pointer lock.`가 발생했습니다. locator 클릭과 좌표 클릭에서 같은 오류가 발생했습니다. 브라우저 제어 도구·창 포커스와의 관계는 아직 확정하지 않았습니다. 실제 포인터 잠금 카메라 검증은 미완료이며, 슬라이더 성공으로 대신하지 않습니다. 검증 후 게임은 EXITED, owner=None으로 정리했습니다.

카메라 잠금 요청을 클릭 처리 중 직접 호출하고, 성공·해제·실패를 별도 camera-status에 표시하도록 변경했습니다. 폴링 상태 갱신이 오류 안내를 덮지 않습니다. 실패 로그에는 문서 포커스와 visibility를 남깁니다. W3C Pointer Lock 명세(https://www.w3.org/TR/pointerlock-2/)의 포커스 요구사항을 참고했으며 브라우저 제한을 우회하지 않습니다. 콘솔 테스트는 WrongDocumentError의 안내와 로그 경로를 통과했습니다.

진단 실행 `a713e162-83b0-439c-bcdd-60e3adb514e9`에서 실제 AX 버튼 클릭으로 WrongDocumentError를 재현했고 documentFocused=true, visibility=visible을 확인했습니다. 따라서 단순히 document.hasFocus=false가 원인이라고 판단할 수 없습니다. OS 창 포커스 또는 브라우저 자동화 환경과의 차이를 추가 조사해야 합니다. 별도 오류 안내는 실제 콘솔에 표시됐고 게임은 EXITED/owner=None으로 정리됐습니다.

실제 Chrome 앱의 창 Raise 후 네이티브 AX 클릭으로 카메라 잠금에 성공했습니다. 실행 `86c29dbe-8b6c-4e9c-954b-b52863e240b3`에서 타이틀과 TutorialScene 모두 잠금 안내가 나타났고 Esc 해제도 확인했습니다. 기존 WrongDocumentError는 동일 코드의 네이티브 창 활성화 경로에서 재현되지 않았으므로, 브라우저 도구 경로와 창 활성화의 차이를 고려해야 합니다. 다만 네이티브 drag 도구의 이동 후에도 lookDelta 명령은 SQLite 이력에 없었으며 플레이어 yaw는 0으로 유지됐습니다. 실제 상대 마우스 이동 검증까지 통과한 것은 아닙니다. 전후 관측은 camera-before.json/camera-after.json에 보존했습니다. 입력 이벤트→델타 합산→전송→중복 미전송·잠금 해제를 실제 console.js VM 테스트로 확인했으며, 물리 마우스 입력 검증을 대신하지 않습니다. 검증 프로세스는 EXITED/owner=None으로 정리했습니다.

### 역할 담당자 이탈 검증과 이력 저장 경합

`test/live-role-disconnect.ts`를 추가했습니다. 기존 Local 호환 역할 그래프에서 P4의 선택지 표시를 확인한 뒤 P4 프로세스를 종료하고, 남은 세 참가자의 분기 완료와 합류를 검증합니다. 종료된 역할을 성공 완료로 기록하지 않는지도 확인합니다. 재접속·늦은 참가와 서버 단일 권위 모드는 이 테스트의 검증 범위에 포함되지 않습니다.

첫 실행 `92de8f8b-c12e-4b04-a422-3b13fb40c6a5`는 역할 검증 전 SQLite `database is locked` 오류로 실패했습니다. 이력 저장소에 최대 250ms의 잠금 대기를 추가했고, 별도 작업자가 보유한 쓰기 잠금을 해제한 뒤 증거가 저장되는 회귀 테스트가 통과했습니다. 재실행 `eef15908-1bb5-4c80-89cc-8b4e0bbca9e2`는 P3 접속 준비 단계에서 전체 시작 제한 시간을 초과했습니다. 두 실행 모두 4개 프로세스를 정리했으며 정리 오류가 없습니다. 역할 이탈 동작은 아직 합격으로 판정하지 않습니다.

재실행과 일반 테스트를 동시에 진행했을 때 HTTP 시작, 자식 프로세스 시작, 취소 지연, UDP 큐 테스트에서도 시간 초과 또는 시간 관련 판정 실패가 발생했습니다. 이후 프로세스 조회에서는 macOS 저장 공간 관리·파일 동기화 작업 등의 높은 CPU 사용량이 관측됐습니다. 이것만으로 모든 실패 원인을 확정하지 않으며, 일반 테스트를 게임 종료 후 순차 실행하여 구분합니다.

게임 종료 후 `node --test --test-concurrency=1 test/*.test.ts`로 실행한 일반 테스트 55개가 모두 통과했습니다. 타입 검사와 공백 검증도 통과했습니다. 동시 부하가 있었던 최초 일반 테스트 실패 기록은 `/tmp/e2e-store-full-tests.log`에 남겨 두었습니다.

단독 재실행 `f271c6b1-dde7-4bec-ba5d-d0ab8574f1f9`은 통과했습니다. P4의 선택지 표시 후 프로세스 종료, 서버 접속자 3명 확인, P1~P3의 각 분기 완료와 합류, 이탈한 역할의 성공 완료 미기록을 검증했습니다. 61개 기록 단계가 통과했으며 4개 프로세스가 모두 종료됐고 정리 오류는 없습니다. 최초 실패와 재실행 결과를 함께 보존합니다. 이 결과는 Local 호환 모드의 역할 이탈 검증이며 재접속은 아직 검증하지 않았습니다.

### 늦은 참가자의 이동 상태 수신

`joinInstances`에 참가를 미룰 클라이언트 목록을 지정하는 기능을 추가했습니다. 호스트, 존재하지 않는 참가자, 중복 지정은 게임 제어 전에 거부합니다. 기본 호출의 전체 참가 동작은 유지하며, 준비 결과와 증거에는 먼저 참가한 대상과 타이틀에 대기한 대상을 구분합니다.

`test/live-late-join.ts`의 실제 macOS 실행 `2e3581c6-d6d8-4814-be14-cd887e7db631`이 통과했습니다. P4가 IntroScene에 남아 있는 동안 P1~P3가 접속하고, P2가 정상 S 입력으로 1m 이상 이동한 뒤 P4를 접속 UI로 참가시켰습니다. P4가 받은 P2 위치의 오차가 0.5m 이내이며 네 클라이언트의 플레이어 명단에 중복 소유자 ID가 없고 서버 접속자 수가 4명임을 확인했습니다. 네 프로세스 모두 종료됐으며 정리 오류는 없습니다. 진행 중인 그래프 참여, 역할 재배정, 같은 프로세스의 재접속은 이 실행으로 검증하지 않았습니다.

### 서비스 인증 자격의 만료 검사

`E2E_CREDENTIAL_EXPIRES_AT`로 선택적인 서비스 토큰 만료 시점을 지정할 수 있도록 추가했습니다. 정확한 만료 경계와 잘못된 설정 거부를 단위 테스트로 확인했으며, 실제 HTTP 서비스에서 만료 전 시작해 만료 후 본문 전송이 끝난 요청과 만료 후 새 요청의 HTTP 401 응답을 확인했습니다. 운영자와 관찰자 모두 적용됩니다. 전체 자동 테스트 57개, 타입 검사, 공백 검증이 통과했습니다. 실행 중인 기존 콘솔 서비스에는 재시작 전까지 이 변경이 적용되지 않습니다.

세부 권한을 도구 이름만으로 분리하면 일반 채팅 입력을 통한 fixture 명령 실행 경로가 남습니다. 따라서 이번 변경을 fixture·프로토콜 권한 분리 완료로 취급하지 않습니다. Unity 명령 처리 경로의 권한 적용, 실행별 자격 발급·접근 범위 제한은 남아 있으며, 인증 만료가 이미 시작된 테스트를 취소하는 것은 아닙니다.

### 실행 단위 채팅 명령 허용 설정

일반 키·텍스트 입력으로 fixture 채팅 명령을 제출하는 우회 경로를 확인하고, `ChatService.HandleLocalSubmission`에서 서버 RPC 호출 전에 자동화 실행의 허용 설정을 확인하도록 추가했습니다. `fixture.allowChatCommands`가 명시적으로 true일 때만 실행 관리자가 허용 환경 변수를 전달하며, 기본값·false·상위 환경 변수 상속을 자식 프로세스 테스트로 검증했습니다. 거부 시 명령 본문을 로그에 남기지 않고 `permission.denied` 이벤트와 화면 안내를 제공합니다.

자동 테스트 58개와 타입 검사, C# 직접 컴파일이 통과했습니다. Unity 에디터 네이티브 테스트는 2026-09-07 23:23:17 UTC에 새 설정 검증을 포함한 9개가 통과했습니다. 새 빌드에서 실제 채팅 명령의 차단·허용 검증은 아직 남아 있습니다. 기존 Build/E2E.app에는 재빌드 전까지 이 변경이 적용되지 않습니다. 사용자별 권한 전달·프로토콜 권한 분리와는 구분합니다.

새 macOS 자동화 빌드 후 `test/live-chat-policy.ts`를 실행했습니다. 허용=False 실행 `d53d4abd-6c71-41c9-87c6-be67dd942690` 결과=passed; 허용=True 실행 `79bffae8-efdc-460c-9884-566a4dcb6908` 결과=passed. 두 실행 모두 정리 오류 없이 종료됐습니다. 차단 실행은 일반 채팅 UI에서 `/tag` 명령을 제출한 뒤 `permission.denied` 이벤트와 태그 미변경을 확인했고, 허용 실행은 같은 경로로 실제 태그 변경을 확인했습니다. 사용자별 권한 분리 및 원격 클라이언트별 권한 적용은 여전히 별도 검증·구현 대상입니다.

### 실제 게임 UDP 복합 장애

`node test/live-network-fault.ts config.json combined` 실행 `8804f505-ecbd-4a31-8e4f-a34173598b7f`이 통과했습니다. P2에 100ms 지연·±40ms 지터·10% 손실·16,384 bytes/s 제한을 6초간 함께 설정했습니다. 장애 구간에서 HTTP 관측 18개를 수집했으며 프레임은 476에서 549로 진행했습니다. 최대 대기 패킷 12개·447바이트를 관측했고 장애로 15개 데이터그램이 폐기됐습니다. 큐 초과 폐기·송신 오류는 없었습니다.

장애 만료 후 정상 입력으로 이동한 위치가 호스트 관측과 0.5m 이내로 다시 일치했고 서버의 4인 연결이 유지됐습니다. P3/P4 프록시의 장애 폐기는 0개였으며, 네 프로세스 모두 정리 오류 없이 종료됐습니다. 이 실행은 해당 복합 설정에서 기능과 회복이 유지된다는 증거입니다. 부하가 대역폭 상한을 지속적으로 넘었는지, 실제 지연 분포가 설정값과 일치하는지를 계측한 성능 검증으로 확대 해석하지 않습니다.

### 프로세스 크래시와 종료 후 지연 응답

프로세스 종료 시 입력 소유권을 None으로 바꾸고 heartbeat와 이벤트 조회 타이머를 모두 정리하도록 수정했습니다. 이미 보낸 관측·제어 응답이 종료 후 도착하더라도 종료 상태에서 제어권이나 이벤트 수집을 되살리지 않습니다. 늦은 관측 응답 회귀 테스트를 포함한 일반 테스트 59개와 타입 검사가 통과했습니다.

`test/live-process-crash.ts`는 정상적으로 접속한 4인 중 P2 프로세스에 SIGKILL을 주입하고, CRASHED·종료 신호 보존, 브리지 관측 실패, 남은 3명과 서버 진행을 확인합니다. 첫 실행 `9407eb4e-0a67-478a-b0db-d034c4b240c8`은 크래시 후 관측 오류의 표시 문자열을 검사한 테스트 오류로 중단됐습니다. 실제 오류 코드 `INSTANCE_UNAVAILABLE`을 검사하도록 수정했으며 첫 실행의 증거를 보존합니다. 첫 실행 프로세스는 모두 종료됐고 정리 오류는 없습니다.

수정 후 실제 크래시 재실행 `1a7e221e-1dae-4f91-a892-4bb3b717dfd4`이 통과했습니다. P2는 CRASHED 및 SIGKILL 상태를 유지했고 제어권과 조회 타이머가 정리됐습니다. P1/P3/P4는 준비 상태와 3인 명단을 유지했으며 호스트의 프레임과 서버 틱이 계속 진행했습니다. 모든 테스트 프로세스가 종료됐고 정리 오류는 없습니다. 이 검증은 의도적으로 주입한 프로세스 종료이며 자연 발생 크래시의 제품 원인을 판정한 결과는 아닙니다.

### 일반 빌드 제거 검사의 범위 확대

브리지 클래스 하나만 검사하던 빌드 후 검사를 확대했습니다. 일반 빌드의 자동화 런타임 타입, 관측 메서드, 입력 교체 필드, `UNITY_E2E_` 환경 변수 문자열 접근을 검사합니다. 실제 게임의 Legacy 입력 및 저장소 래퍼는 허용하며 계측 전용 코드만 제거 대상으로 다룹니다. 계측 빌드는 필수 구성요소 8개의 존재를 확인합니다.

브리지 없이 다른 자동화 코드만 남은 합성 어셈블리를 검출하는 테스트를 포함해 네이티브 테스트 10개가 2026-09-07 23:36:11 UTC에 통과했습니다. 이어 최신 소스의 `Build/E2E-removal-check.app` 일반 macOS 빌드가 강화한 검사를 통과했습니다. 결과 어셈블리 SHA-256과 검증 범위는 `artifacts/unity-e2e/ordinary-removal-verification.json`에 저장했습니다. CI 실행 명령을 README에 추가했지만 실제 CI 실행 노드에서 실행한 결과로 취급하지 않습니다.

### 콘솔 캡처 갱신 분리

상태·이벤트 조회와 캡처를 분리하고 목표 12 FPS로 독립 예약합니다. 중복 캡처는 한 건으로 제한하며 이미지 해독 후 화면과 클릭 기준 프레임을 함께 갱신합니다. 인스턴스 선택 변경 중 이전 응답이 도착하면 폐기하고, 기존 화면을 지워 다른 플레이어의 화면으로 잘못 클릭하지 않도록 했습니다. 중복 요청 억제와 이전 선택의 지연 프레임 폐기를 기존 입력 VM 테스트에 추가했습니다. 썸네일 갱신 예약은 1초로 줄였습니다.

실제 단일 튜토리얼 실행 `ef58f09e-4e43-43d5-a887-2fde1a8b9765`에서 Chrome의 해독 완료 기준 화면 속도 11.2 FPS를 관측했습니다. SQLite의 최근 9.974초 캡처 성공 112건 기준 속도는 11.13 FPS이며 최대 요청 시간은 56ms였습니다. 측정 근거는 실행 폴더의 `console-stream-measurement.json`에 저장했습니다. 인스턴스는 종료됐고 콘솔이 실행 화면 없는 대기 상태로 돌아온 것을 확인했습니다. 이 측정은 단일 로컬 세션이며 4인 부하와 원격 네트워크의 전송률을 증명하지 않습니다.

### 2026-09-08: 4인 캡처 처리량과 백그라운드 UI 입력

- `5139cd98-a6a2-45f8-87eb-539ee17eb942/stream-report.json`: 일반 실행 경로에서 30초 동안 주 화면 11.22 FPS, 다른 세 화면 약 0.99 FPS를 기록했다. 각 인스턴스의 플레이어 4명 유지 검사가 통과했고, 종료 후 모두 `EXITED`, 정리 오류는 없었다.
- 브라우저 해독·표시를 제외한 브리지 캡처 처리량 결과다. 게임 30 FPS나 장시간 안정성을 입증하지 않는다.
- 브리지 요청에 `Connection: close`를 적용했다. 비교 실험 `9ffa0f67-91de-4aaf-b8a6-c775fa8f54b3`에서는 11.55 FPS였으며, 이전 연결 재사용 실행에서는 5.20~8.23 FPS로 목표에 미달했다. 부하 차이가 있으므로 이 수치를 일정한 개선 비율로 해석하지 않는다.
- 작은 창의 타이틀 메뉴에 세로 스크롤을 추가했다. 러너는 메뉴 영역에 포인터를 옮겨 휠 입력을 보내고 버튼의 입력 가능 상태를 다시 확인한다. 화면 밖 중심점은 클릭 가능 상태로 보고하지 않는다.
- P3 가상 입력 장치가 백그라운드에서 비활성화된 증거를 확보한 뒤, 자동화 전용 Mouse/Keyboard 레이아웃에 백그라운드 동작 속성을 지정했다. 위 실기동에서 네 플레이어의 접속 절차가 통과했다.

재실행: `node test/live-stream.ts config.json` (`Tools/unity-e2e`에서 실행). 보고서의 단계별 시간은 응답 헤더 대기와 Unity 내부 처리 시간이 겹치므로 전체 합으로 계산하지 않는다.

### 2026-09-08: 현재 빌드의 핵심 시나리오 재검증

- 프로세스 실행 `9bc066b6-380f-4fd9-baa3-d06841fb4db3`, 시나리오 실행 `28216c59-f3d4-4943-97af-22e94862afde`: `four-player-core.e2e.json` 26단계 통과, 정리 오류 없음. 이는 현재 빌드의 1회 실행 결과이며 이전 빌드의 20회 반복 결과를 대체하는 20회 검증은 아니다.
- 설정창 인계 실행 `8b84a5f2-0f86-43f4-861a-5bcf761fce90`: 메뉴 스크롤부터 `RemoteHuman` 인계까지 5단계 통과. 증거 저장 후 테스트 프로세스를 종료했다.
- 외부 서비스 전체 테스트 62개 통과. 일반 `uiAction`과 접속 절차가 공통 UI 탐색 함수를 사용하며, 휠 입력 후 버튼 상태를 다시 확인하는 순서와 취소 처리를 테스트한다.

### 실패 직후 4인 입력 해제

실제 4인 실행 `df869e5a-35e2-4886-91d1-52a865179908`에서 각 플레이어의 W 입력 적용을 확인한 뒤 의도적으로 assertion을 실패시켰습니다. 러너의 실패 증거 수집 진입 시 네 플레이어 모두 W 입력이 해제돼 있었고, 최종 제어권은 모두 `None`이었습니다. 최초 `ASSERTION_FAILED`가 유지됐으며 정리 오류가 없고 네 프로세스가 모두 종료됐습니다. 증거는 실행 폴더의 `runner-failure-release-report.json`, `injected-failure-result.json`, 플레이어별 `*-before-evidence-state.json`에 보존했습니다.

단일 실행 `b0badc02-15ba-48a1-bce0-f3d6e0e98ae8`도 같은 순서 검사를 통과했습니다. 전체 서비스 테스트 116개가 통과했습니다. 이 결과는 실패 후 입력 정리 순서를 검증한 것이며, 반복 실행의 제어권 만료 원인을 해결했다는 의미는 아닙니다.

### CPU 프로파일을 수집한 20회 반복 통과

Suite `6968905c-218f-4b5b-b3a3-1d365d19c8be`는 `mac_direct`의 `four-player-core.e2e.json`을 20회 연속 통과했습니다. 모든 회차의 정리 오류가 없고, 각 회차의 네 플레이어가 모두 `EXITED`인 것을 `shutdown.json`으로 확인했습니다. CLI 종료 코드는 0입니다. 이번 실행에는 Node `--cpu-prof`가 적용됐고, 실행 도중 코드나 임대 시간·실패 판정 기준을 변경하지 않았습니다.

프로파일 수집 기간은 약 23.77분이고 659,375개 표본 중 614,561개가 idle이었습니다. 최대 표본 간격은 약 2.59초이며, 회차별 자원 기록의 최대 이벤트 루프 지연은 약 4.46초, 최대 heartbeat 왕복 시간은 5.44초였습니다. 이 최댓값은 실행 전체 구간에서 집계되므로 유효한 제어권 유지 중 동일한 지연이 발생했다는 뜻은 아닙니다. CPU 표본 비율을 벽시계 시간 비율이나 OS 정지 원인의 증거로 해석하지 않습니다. 이번 통과만으로 이전 제어권 만료 원인이 해결됐다고 판단하지 않습니다.

원본 프로파일 `runner.cpuprofile`과 표본 집계·회차별 종료 검증 `profile-gate-analysis.json`을 suite 폴더에 보존했습니다. 실제 AI 워커 연결, Windows 실행 등 나머지 미완료 범위는 그대로 남아 있습니다.

### 실제 키 조합 기록과 재생

`test/live-recorded-chord.ts` 실행 `e4a1e0ff-9079-4c64-bdba-b2eaff491c87`이 통과했습니다. 일반 타이틀 UI로 호스트 게임을 시작한 뒤 W를 누르고 A를 추가한 다음 W, A 순서로 해제하는 조작을 기록했습니다. 생성된 초안을 러너로 재생하면서 W 단독 → W+A → A 단독 → 모두 해제 순서를 관측했습니다. 시나리오가 통과했고 정리 오류 없이 플레이어가 종료됐습니다. 원본 기록·초안과 `chord-replay-samples.json`, `chord-replay-result.json`, `chord-report.json`을 실행 폴더에 보존했습니다.

이 결과는 단일 macOS 플레이어에서 API 입력 기록을 재생한 검증입니다. 브라우저의 실제 키 이벤트 수집이나 4인 부하 중 동일 타이밍을 검증한 결과로 확대하지 않습니다. 입력 `wait`는 프레임마다 제어권·기한을 검사하고 키 만료를 연장하지 않습니다. 런타임 대기 인수 검증을 포함한 Unity 테스트 14개가 2026-09-08 08:55:15 UTC에 통과했습니다. 해당 변경을 포함한 macOS 자동화 빌드의 완료는 Editor.log 97331행에 기록됐습니다.

### 대기 중 입력 취소

실제 macOS 실행 `10a44b0b-909b-43f3-ad15-3c2750fab490`에서 `test/live-wait-cancel.ts`가 통과했습니다. W 입력 적용 후 1.5초 대기하고 A를 누르는 시퀀스를 실행한 뒤, 대기 중 `input.release_all` 또는 `control.handoff(None)`을 각각 요청했습니다. 두 경우 모두 진행 중 시퀀스가 `CANCELLED`로 끝났습니다. 원래 대기 종료 시점을 넘어서까지 W 해제와 수평 입력 0을 관측했으며, 제어권 반환의 경우 소유자 `None`도 유지됐습니다. 모든 테스트 플레이어가 종료됐습니다. 근거는 `wait-cancel-samples.json`과 `wait-cancel-report.json`입니다. 이 검증은 명시적인 입력 해제·제어권 반환이며, HTTP 연결만 끊었을 때의 동작과는 구분합니다.

### 4인 환경에서 원격 플레이어 기록 재생

`node test/live-recorded-chord.ts config.json four` 실행 `abe90be0-830b-4009-b315-585d63809fe7`이 통과했습니다. 호스트와 클라이언트 3명이 접속한 상태에서 P2의 키 조합을 기록·변환·재생했고, P2에서 W 단독 → W+A → A 단독 → 모두 해제 순서를 관측했습니다. 종료 직전 나머지 세 플레이어의 W·수평·수직 입력은 모두 해제 상태였고 서버 연결 수는 4였습니다. 네 프로세스가 모두 종료됐습니다. 다른 플레이어의 입력을 재생 전 구간에 걸쳐 수집한 검증은 아니므로, 일시적인 교차 입력까지 없었다는 증거로 해석하지 않습니다. 실행 폴더의 기록 초안, `chord-replay-samples.json`, `chord-final-*.json`, `chord-report.json`을 보존했습니다.

4인 입력 격리 관측을 추가한 실행 `9aa81cf8-45c8-4885-9b4b-e180f743e865`가 통과했습니다. 재생 중·완료 직후 네 플레이어를 함께 관측한 표본은 36개이며, 다른 세 플레이어의 서로 다른 프레임 수는 [36, 36, 36]개였습니다. 수집한 모든 상대 플레이어 표본에서 W·수평·수직 입력이 해제 상태였고, P2 키 조합 순서와 서버 4인 연결 유지도 통과했습니다. 네 프로세스 모두 종료됐습니다. 표본 사이의 모든 프레임까지 관측한 결과는 아닙니다. 직전 실행 `0518fdb1-5bb3-4d7f-8ee9-73685525c186`은 완료 직후 마지막 해제 표본을 수집하지 않아 실패했으며, 해당 증거를 보존하고 완료 후 명시적 관측을 추가했습니다.

4인 이동·카메라 기록 재생 실행 `e10c9e39-a99e-42fd-b93b-c0fc93579b40`이 통과했습니다. P2가 W와 A를 유지하는 동안 `lookDelta`를 기록했고, 생성된 초안에 카메라 입력이 포함됐음을 확인했습니다. 재생 중 W가 눌린 표본에서 시작 시점 대비 카메라 yaw가 0.5도 넘게 변한 것을 관측했습니다. 키 조합 상태 순서, 다른 세 플레이어의 표본별 이동 입력 해제, 4인 연결 유지와 전체 프로세스 종료도 통과했습니다. 이는 API 입력 기록의 재생 검증이며 브라우저 포인터 잠금 이벤트 전달 검증을 대신하지 않습니다. 휠 입력 변환은 단위 테스트로 확인했으며, 이번 실제 실행에는 휠을 포함하지 않았습니다.

브라우저 카메라 검증 도구 범위를 재확인했습니다. 현재 연결된 in-app 브라우저의 공개 Playwright API에는 클릭·키 입력이 있지만 상대 마우스 이동 API가 없습니다. 따라서 해당 API로 포인터 잠금 상태의 실제 이동 전달을 검증하지 못했습니다. 콘솔은 잠긴 화면의 `mousemove`와 유효한 `movementX/Y`를 누적해 전달하며, 이 경로의 합성 이벤트 테스트와 Unity 직접 입력 검증은 실제 브라우저 마우스 전달의 증거로 대신하지 않습니다. 새로운 실제 브라우저 카메라 통과 결과는 추가하지 않았습니다.

### 같은 방에서 플레이 회귀 반복

`soak.ts`에 정의 파일을 지정해 같은 프로세스·같은 방에서 회귀를 반복하는 `active_regression` 모드를 추가했습니다. 첫 실행 `95f7c35e-80ed-4158-b47e-5f66534fe7fe`은 두 번째 회귀의 `inventory_visible`에서 시간 초과로 끝났습니다. 실패 관측의 입력 문맥은 `DialoguePanelUIController`였고 대화를 진행할 수 있는 상태였습니다. 기존 핵심 예제가 accepted 노드 도달까지만 검사하면서 마지막 대화문을 남긴 결과입니다.

장시간 실행용 `four-player-active-soak.e2e.json`은 마지막 대화문을 정상 입력으로 완료하고 `Gameplay` 문맥 복귀까지 기다립니다. 수정한 1분 실행 `f48a0537-4061-4dcc-b318-cf61983c7275`은 같은 방에서 6회 회귀를 통과했고 정리 오류가 없었습니다. 이는 1분 동안 UI·대화 경로를 반복한 검사이며, 60분 또는 이동을 포함한 전체 플레이 검증은 아직 아닙니다. 첫 실패와 회차별 결과·방 상태·종료 보고서를 모두 보존했습니다.

### 60분 활성 회귀의 제어권 상실

실행 `0aed3599-da89-4ce7-bf71-7d33793bc763`은 19회귀 통과 후 20회귀의 `select_accept`에서 `CONTROL_DENIED`로 중단됐습니다. 마지막 정상 방 검사 시점은 약 6분 18초였습니다. 실패 회귀는 `fe818471-9588-4f2b-90f0-deba41049558`이며 P1·P3의 즉시 입력 해제는 `STALE_CONTROL_EPOCH`로 거부됐습니다. 최종 프로세스 정리 오류는 없고 네 프로세스가 모두 종료됐습니다. 60분 통과 결과가 아닙니다.

실패 직전 P1·P3의 성공 heartbeat 응답 소요 시간은 각각 2,170ms·2,207ms였고 다음 heartbeat가 이전 세대 오류로 거부됐습니다. 기존 갱신 루프는 한 응답을 기다리는 동안 다음 전송을 건너뛰었습니다. Automation 갱신을 같은 epoch의 최대 두 요청까지 허용하도록 보완했습니다. 요청 TTL 1초, 전송 주기 500ms, Unity 임대 2초는 유지하며 다른 epoch를 자동으로 갱신하거나 재획득하지 않습니다. 지연 응답 중 두 요청 상한과 epoch 변경 후 중단 테스트가 통과했습니다. 수정 후 실제 지속 실행 검증과 지연의 OS 원인 판정은 아직 남아 있습니다.

### 두 요청 heartbeat 보완 후 지속 실행 실패

실행 `343cc2b5-5593-4dd8-9037-760435644797`은 10회귀 통과 후 11회귀의 `inventory_close`에서 `CONTROL_DENIED`로 중단됐습니다. 마지막 정상 방 검사 시점은 약 2분 23초였습니다. 실패 회귀는 `f6ad89a0-a711-472f-b1da-a03031cf7e4d`이고, 네 프로세스가 모두 종료됐으며 최종 프로세스 정리 오류는 없었습니다. 즉시 입력 해제의 이전 세대 거부는 실패 회귀 보고서에 그대로 보존합니다.

실패 직전 heartbeat에서 P2의 응답 소요 시간은 6,235ms, P3·P4의 타임아웃 응답은 각각 5,694ms·5,692ms였습니다. P1은 09:32:12.371 UTC의 요청 뒤 다음 시작이 09:32:14.331 UTC였으며, 전송 주기 자체에도 지연이 있었습니다. 동시에 진행할 수 있는 요청 수를 늘리는 것만으로 실제 지속 실행의 제어권 상실을 해결하지 못했습니다.

종료 후 확인한 볼륨 사용률은 98%, 가용 공간은 약 12GiB이고 `memory_pressure -Q`의 여유율은 63%였습니다. 이는 실패 중의 동시 측정이 아니며 원인 확정 자료가 아닙니다. E2E 증거 디렉터리는 약 1.4GiB, Build와 Library는 각각 약 14GiB였습니다. 사용자 파일이나 과거 실패 증거는 삭제하지 않았습니다. 같은 조건에서 통과할 때까지 반복하는 대신 60분 활성 회귀를 미완료로 유지합니다.

### 좌표 클릭에서 UI 식별자 초안 생성

실행 `152894ad-ce21-4616-9b56-c3c50ef6d8c9`에서 `test/live-recorded-click.ts`가 통과했습니다. 타이틀 플레이 버튼에 실제 포인터 눌림·해제를 보냈고 두 응답의 버튼 식별자가 같음을 확인했습니다. 기록 초안은 `btnPlay`를 대상으로 하는 UI 조작 하나를 생성했습니다. 이전 메뉴로 정상 복귀한 뒤 초안을 재생해 `btnHost` 표시를 확인했으며, 시나리오 통과·정리 오류 없음·플레이어 종료를 확인했습니다. `click-targets.json`, 원본 기록·초안, `click-replay-result.json`, `click-report.json`에 증거를 보존했습니다.

첫 실행 `198efa70-f078-442b-9bb6-ca3cc2e6db11`은 씬 진입만 기다린 뒤 버튼의 조작 가능 상태를 확인하지 못해 실패했습니다. 기존 UI 탐색의 준비 대기·스크롤 처리를 `waitForInteractable`로 분리해 사용한 뒤 통과했습니다. UI 탐색 테스트 4개와 타입 검사도 통과했습니다. 이 실행은 단일 macOS 플레이어의 API 좌표 입력이며, 모든 버튼 종류·패널 겹침·브라우저 물리 클릭을 검증한 결과는 아닙니다. 빌드 완료는 Editor.log 98402행에 기록됐습니다.

설정 버튼 실행 `178b1962-4a3b-4564-b1cf-7432fbcb2e44`도 좌표 클릭 기록·식별자 변환·재생을 통과했습니다. `waitForInteractable`로 설정 버튼을 준비한 뒤 기록을 시작하고, 눌림·해제의 `btnSettings` 식별자를 확인했습니다. 설정창을 정상 닫은 뒤 생성된 UI 조작을 재생해 `settings-root` 표시를 확인했으며 플레이어가 종료됐습니다. 준비 단계의 스크롤은 기록 시작 전에 수행되므로 스크롤 자체의 기록 변환 검증은 아닙니다. 실행 명령은 `node test/live-recorded-click.ts config.json settings`입니다.

### 활성 soak의 자원 진단 기록 추가

`soak.ts`도 `soak-resources.json`에 2초 간격의 최근 300개 표본을 저장하도록 변경했습니다. UTC 시각, 경과 시간, 실행 단계, 완료한 반복 횟수, 콜백 지연, Node CPU 사용량·메모리, 시스템 여유 메모리·부하, 구간별 이벤트 루프 지연과 OS 메모리 압력을 기록합니다. 실패 증거 수집 및 종료 정리에 들어가기 전에 표본을 남깁니다. 지연된 콜백은 단계 경계를 넘을 수 있으므로 단계 정보만으로 원인을 단정할 수 없습니다. 타입 검사와 변경 형식 검사가 통과했습니다. 이 변경으로 60분 활성 회귀가 통과했다고 판단하지 않으며, 새 진단 파일의 실제 Unity 실행 검증은 남아 있습니다.

실제 macOS 4인 실행 `383f8621-fe11-4e56-b0cb-1abb70c62a33`은 준비 후 1분 동안 회귀 6회를 통과했습니다. `soak-resources.json`에서 자원 표본 46개, 메모리 압력 표본 9개와 준비·활성 회귀·연결 확인·정리 단계를 확인했습니다. 최대 표본 콜백 지연은 약 16.4ms였습니다. 정리 오류 없이 네 프로세스가 모두 종료됐고 실행 명령도 종료 코드 0으로 끝났습니다. 이 결과는 새 진단 기록의 정상 실행 검증이며, 기존 장시간 제어권 상실이 해결됐다는 증거는 아닙니다.

### 자원 진단을 포함한 60분 활성 회귀 시도

실행 `84f2e9b3-733c-4661-bfdc-aa5555720d9d`은 4회 통과 후 5회차(`f26163bb-8b6a-4652-bfb5-eb5bd1724597`)에서 `INSTANCE_UNAVAILABLE`로 중단됐습니다. 네 플레이어의 즉시 입력 해제에서 `STALE_CONTROL_EPOCH`가 기록됐지만 최종 프로세스 정리 오류는 없었고 모두 EXITED였습니다. 명령은 종료 코드 1로 끝났습니다.

P3 heartbeat는 10:02:15.393 UTC에 시작해 16.219에 STALE_CONTROL_EPOCH로 끝났습니다. 15.402의 자원 표본은 콜백 간격 3,390ms, 예정 간격 대비 지연 1,390ms, 구간 이벤트 루프 최대 지연 1,568ms, 시스템 1분 부하 93.46을 기록했습니다. 이후 P1·P4 heartbeat는 약 5.8초 후 타임아웃으로 끝났습니다. 실행 전체의 더 큰 지연은 첫 제어권 상실 이후에도 발생했으므로 이를 최초 원인으로 단정하지 않습니다. 높은 시스템 부하와 Node 스케줄링 지연이 같은 시점에 관측됐지만 특정 프로세스나 OS 동작이 원인이라는 증거는 아직 없습니다. 60분 활성 회귀는 미통과이며 동일 조건의 반복 재시도는 진행하지 않았습니다.

P3의 마지막 정상 heartbeat는 10:02:13.603에 시작해 13.784에 끝났으며 다음 시작은 15.393이었습니다(전송 간격 1,790ms). 실패 요청의 Unity 큐 대기는 52.7ms이고 HTTP 헤더 대기는 824.6ms였습니다. 이에 따라 soak 진단에 도구가 직접 실행한 생존 Unity PID만을 대상으로 10초 간격 CPU·RSS 표본을 추가했습니다. 조회는 최대 하나만 진행하고 2초 제한 및 최근 300개 보관을 적용했습니다. CPU 비율은 ps의 OS별 정의이며 여러 코어를 사용하면 100을 넘을 수 있습니다. 파싱·빈 대상 처리 테스트 2개와 타입 검사는 통과했으나 새 프로세스 표본의 Unity 실기 검증은 남아 있습니다.

프로세스 진단 실기 실행 `4ee76f41-fe02-4421-8633-9b51188c4714`은 준비 후 1분 동안 회귀 5회를 통과했고 정리 오류 없이 네 프로세스가 종료됐습니다. 프로세스 표본 8개가 모두 조회 가능 상태였고, 표본에 등장한 PID 집합은 실행 기록의 네 Unity PID와 정확히 일치했습니다. RSS가 양수이고 CPU 비율이 유효함을 확인했습니다. 별도의 제한된 실행 환경에서는 `ps` 생성이 EPERM으로 실패하는 경로를 확인했습니다. 이때 Promise 거부가 전파되지 않고 조회 불가로 반환되도록 수정한 뒤 해당 경로와 타입 검사·단위 테스트 2개가 통과했습니다. 실행 중인 Unity 검증은 이 예외 처리 수정 전 모듈을 사용했으며, 정상 조회 경로의 실기 증거입니다.

### 추가 목표: 환자 시나리오의 네 역할 전체 퀘스트 완료

사용자가 patient_a_critical 및 patient_b_c_ct에서 P1~P4가 각각 /a·/b·/c·/d를 입력하고 모든 요구 퀘스트를 중단 없이 완료하는 동적 E2E를 요청했습니다. 아이템 획득만으로 통과시키지 않습니다. 전체 퀘스트 정의와 노드·소스 해시를 `examples/patient-full-play.requirements.json`에 보존했습니다(A 27개, B·C 21개 정의). 역할별 활성 분기와 배정된 퀘스트의 완료 증거 및 시나리오 종료 증거가 필요하며 현재 미검증입니다. StaticPlacedItem의 transform.position과 보상 목록을 읽는 관측 필드 및 엔티티 ID 기반 staticItem 이동을 추가했습니다. 타입 검사와 직접 C# 컴파일을 수행했으며 실제 빌드·플레이 검증은 남아 있습니다.

staticItem 이동의 소실·중복 엔티티 거부 및 고유 엔티티 도착 테스트가 통과했습니다. 새 관측 필드를 반영하는 macOS 자동화 플레이어 빌드를 Unity 메뉴에서 시작했으며 이 기록 시점에는 UnityLinker 단계였습니다. 전체 환자 시나리오 플레이의 통과 증거로 사용하지 않습니다.

환자 시나리오 초기 실기 실행: A `1d6ec74c-7173-450e-a94b-a519001b6770`, B·C `d7af0611-fa01-4522-828a-4ab0cdc74028`. 각각 네 플레이어가 정상 채팅 입력으로 /a·/b·/c·/d를 실행했고 마지막 관측에서 해당 역할 태그를 확인했습니다. 호스트의 정상 채팅 입력으로 시나리오를 시작했으며 정적 아이템 311개가 관측됐습니다. 두 실행 모두 종료 코드 0, 네 플레이어 EXITED를 확인했습니다. 이는 초기 진입 확인이며 스크립트는 fullPlayPassed:false를 명시합니다. A의 초기 노드는 P_START_A, 각 역할의 환자 도착 분기가 활성화됐고 관측 실행 모드는 Local 호환 모드입니다. 전체 퀘스트 및 최종 종료 검증은 미완료입니다.

A 시나리오 도착 이동 시도 `6aa2af99-dd7b-4574-becf-02fbaba789a0`은 이동 전에 중단됐습니다. 시나리오 시작 채팅의 Return 입력이 CANCELLED였고, 같은 P1의 heartbeat에서 타임아웃과 STALE_CONTROL_EPOCH가 발생했습니다. Return의 Unity 처리 시간은 약 1,792ms였습니다. 도착 이동 실패나 게임 퀘스트 교착으로 판정하지 않습니다. 모두 EXITED를 확인했습니다. 진입 스크립트에 실패 단계 및 입력 해제 후 관측 저장을 추가했으며, 제어권 재획득이나 시작 명령의 자동 재전송은 추가하지 않았습니다.

B·C 도착 이동 실행 `67d80ff7-7ccc-4eaf-95c4-0e32a2889df2`은 네 역할 모두 MOVEMENT_BLOCKED였습니다. 마지막 관측에서 안내 대화가 활성화돼 canMove:false, 입력 문맥 DialoguePanelUIController였고 P_ANNOUNCE_ARRIVAL에 있었습니다. 이는 안내를 넘기지 않은 테스트 절차의 누락으로 분류합니다. 정상 대화 진행 입력을 수행한 뒤 Gameplay 문맥에서 이동하도록 스크립트를 보완했습니다. 모두 EXITED였으며 보완 후 실기는 아직 실행하지 않았습니다. 미수행 복구로 진행된 시나리오를 구분하기 위해 recoveryNotes 관측도 추가했고 직접 C# 컴파일이 통과했습니다. 새 관측의 빌드 반영은 남아 있습니다.

B·C 보완 실행 `4a664f0b-d2ef-4da2-8e84-d91198f4d114`은 entry_observation 단계에서 관측 타임아웃으로 중단됐습니다. 실패 후 네 관측 파일과 전체 EXITED를 확인했습니다. 후속 관측 FPS는 약 11.8~15.0이지만 타임아웃 당시 원인으로 단정하지 않습니다. 대화 진행 보완 자체의 성공·실패 판정은 불가능합니다. 초기 관측 48개 파일 쓰기를 제거하고 각 플레이어에서 요청한 그래프와 초기 방송 노드가 관측되는 준비 조건을 기다린 뒤 하나씩 저장하도록 변경했습니다. 새 준비 방식의 실기는 남아 있습니다.

준비 대기 보완 실행 `aa5dbf08-c89f-4e5d-a3ec-54d79581ab62`은 안내 진행 후 P1·P2의 이동이 완료됐고 P3·P4에서 NAVIGATION_STUCK가 기록됐습니다. 이후 관측 중 타임아웃으로 종료했으며 모두 EXITED였습니다. 마지막 P3·P4 위치는 도착 목표에서 수평 약 5.41m·5.56m로 퀘스트의 reachDistance 6m 이내였지만 테스트가 임의로 요구한 4.5m 밖이었습니다. 이동의 도착 반경을 5.8m로 조정했습니다. 이는 퀘스트 완료 증거를 대체하지 않으며 이후 분기 진행 확인이 필요합니다. 각 로컬 QuestManager의 퀘스트 ID·완료 상태·과제·진행량 관측을 추가하고 직접 C# 컴파일을 통과했습니다. 해당 관측의 실제 빌드 반영 및 새 반경 실기는 남아 있습니다.

도착 단계 스크립트의 판정을 강화했습니다. 이동 성공에 더해 각 역할의 초기 분기 이탈, 같은 시나리오의 후속 퀘스트 존재, recoveryNotes 없음이 필요합니다. localQuests 또는 recoveryNotes 관측이 없으면 실패합니다. 기존의 고정 40개 도착 후 파일 쓰기는 제거하고 조건 대기 후 결과 또는 정체 상태를 저장합니다. 타입 검사가 통과했으며 Unity 관측 필드의 새 빌드를 시작했습니다. 실제 판정 검증은 남아 있습니다.

새 관측 빌드 완료는 Editor.log 100530행에 기록됐습니다.
실행 `bbb12c2d-f15f-484c-a84b-e332d3320c9a`은 scenario_start의 CANCELLED로 중단됐고 모두 EXITED였습니다. 실패 후 recoveryNotes는 관측됐으나 localQuests는 null이었습니다. 소스를 확인한 결과 QuestManager는 플레이어 컴포넌트가 아니라 Service 레지스트리에서 조회해야 했습니다. ScenarioController와 동일한 레지스트리 접근으로 수정한 뒤 직접 C# 컴파일이 통과했습니다. 수정 후 빌드·실기 검증은 남아 있습니다.

진입 스크립트의 안내 대화 처리를 Promise.allSettled로 변경해, 한 역할이 실패한 뒤 다른 역할의 입력 처리가 계속되는 동안 종료 정리가 시작되지 않도록 했습니다. 네 역할 결과를 arrival-dialogue.json에 보존하고 실패가 있으면 이동하지 않습니다. 타입 검사 및 변경 형식 검사가 통과했습니다. 서비스 레지스트리 기반 퀘스트 조회 수정의 Unity 빌드를 시작했습니다.

실행 `64861d15-8670-4f3c-9ecf-faf3d5098653`에서 네 역할 모두 안내 및 도착 이동을 통과했고 퀘스트 관측도 정상 동작했습니다. 후속 분기 대기에서는 P1의 ARRIVAL_QUEST_DID_NOT_ADVANCE가 발생했습니다. 실제 원인은 P4의 도착 퀘스트 미완료였으며, 마지막 목표 거리 약 6.035m로 기준 6m 밖이었습니다. P1~P3의 완료 상태는 true이고 복구 기록은 없었습니다. 이동 성공만으로 판정하지 않은 덕분에 미완료 역할을 발견했습니다. 모두 EXITED였습니다. 미완료 역할은 옆으로 이동 후 다시 접근하고 퀘스트 완료를 확인하는 한정 반복을 추가했습니다. 제어권 상실은 재시도하지 않으며, 이 변경의 실기 검증은 남아 있습니다.

실행 `4024423a-cfb9-4638-aeb2-60687348a0f6`에서는 네 역할의 도착 퀘스트가 실제 completed:true였고 안내·이동·확인도 모두 성공했지만 P_ANNOUNCE_ARRIVAL에 남아 ARRIVAL_QUEST_DID_NOT_ADVANCE가 발생했습니다. 모두 EXITED였습니다. 소스와 씬을 대조하면 퀘스트는 목표 반경 6m로 완료되지만 공동 도착 카운터는 별도 ScenarioTriggerZone의 플레이어별 진입 신호를 요구합니다. OverworldScene의 해당 BoxCollider 크기는 8×3×8이고 같은 중심에 있습니다. 따라서 퀘스트 완료만으로 트리거 진입이 보장되지 않는 UI·진행 조건 불일치를 발견했습니다. 플레이 테스트는 플레이어를 좌우로 분산한 뒤 중심 반경 3m까지 접근하도록 확장했습니다. 구역 진입으로 진행되는지의 실기는 아직 남아 있으며 게임 데이터는 변경하지 않았습니다.

구역 안쪽 접근 실행 `9df6e0a4-75e8-4947-85e0-b0a5bfe4d583`에서 네 역할 모두 P_TRIAGE로 넘어갔습니다. P1은 Quest_Triage_Lead, P2~P4는 Quest_Triage_Wait를 받았습니다. P2·P3의 NAVIGATION_STUCK 때문에 명령 종료 코드는 1이었지만, 실패 후 상태는 도착 분기 완료를 입증합니다. 모두 EXITED였습니다. 이동 정체 자체를 무조건 성공 처리하지 않고, 실제 후속 퀘스트와 무복구 진행 조건을 확인하도록 스크립트를 조정했습니다. 분류 UI의 패널·취소·등급 영역에는 안정적인 조회 이름을 추가했으며 이후 빌드 검증이 필요합니다.

환자 접근을 위해 scenarioEntities 관측과 scenarioEntity 이동 대상을 추가했습니다. IScenarioIdentifiedEntity의 식별자와 실제 transform.position을 읽으며 중복 또는 소실된 식별자는 이동 실패로 처리합니다. 기존 NPC 관측에는 의사만 있고 patient_b·patient_c·patient_dummy_d_b가 없었던 점을 보완합니다. 타입 검사, 대상 중복·소실·도착 테스트 1개, 직접 C# 컴파일이 통과했습니다. 환자 분류 UI의 고유 이름과 함께 새 빌드 및 실제 환자 상호작용 검증이 필요합니다.

진입 테스트에 triage 단계를 추가했습니다. B·C 시나리오의 도착 이후 P1이 patient_b·patient_c·patient_dummy_d_b에 접근해 triage_assess를 열고, 데이터의 의도 등급 Level2·Level2·Level5에 해당하는 UI 영역을 포인터로 선택합니다. 전후 관측을 보존하며 아직 이 단계의 실기·제출 승인 확인을 완료하지 않았습니다. 타입 검사가 통과했고 환자 위치 관측·분류 UI 이름을 포함하는 Unity 빌드를 시작했습니다. 전체 퀘스트 통과 판정은 여전히 하지 않습니다.

환자 관측·분류 UI 이름을 반영한 빌드가 Editor.log 102670행에서 성공했습니다. triage 실행 `3a14b390-f6b7-4bc6-9c33-939fb29bb5a0`은 arrival_movement 단계의 관측 타임아웃으로 종료됐고 네 프로세스가 모두 EXITED였습니다. 실패 후 관측에서 모든 클라이언트의 scenarioEntities에 세 환자 ID가 각각 PatientController로 나타났습니다. 환자 위치 관측의 실기 동작은 확인했으나 분류 UI까지 도달하지 못했으므로 분류 제출 성공 증거는 없습니다.

scenarioEntities 관측의 전체 MonoBehaviour 검색을 제거했습니다. PatientController.Network.RegisterEntity 경로를 확인하고 GetAllEntities의 등록된 활성 오브젝트에서 IScenarioIdentifiedEntity와 레지스트리 키가 일치하는 대상만 조회합니다. 환자의 현재 transform을 매 관측에 읽으며 위치를 고정 캐시하지 않습니다. 직접 C# 컴파일과 변경 형식 검사가 통과했습니다. 관측 비용 감소를 의도한 변경이지만 실제 성능 개선이나 제어권 상실 해결을 입증하지는 않았습니다. 새 빌드·실기 검증이 필요합니다.

분류 스크립트는 포인터 입력 직전 이력 커서를 잡고, 이후 해당 환자의 triage_submitted 및 triage_correct 신호가 새로 저장됐는지 확인하도록 강화했습니다. 클릭 응답만으로 제출 성공을 판정하지 않으며 10초 안에 확인하지 못하면 TRIAGE_SUBMISSION_UNCONFIRMED로 종료합니다. 최적화된 엔티티 관측의 Unity 빌드를 시작했습니다. 이 신호 확인 경로의 실제 실행 검증은 남아 있습니다.

레지스트리 관측 빌드는 Editor.log 103740행에서 성공했습니다. 실행 `dfe6ea00-4808-45e4-a491-649aa3aad94f`은 role_commands 단계에서 STALE_CONTROL_EPOCH로 중단됐습니다. P4의 control.acquire(71ms), 채팅 열기 입력(521ms)은 성공했지만 이어진 ui.text가 만료된 epoch로 거부됐습니다. 이는 분류 접근 전의 제어 안정성 실패이며 전체 종료 코드 1, 네 프로세스 EXITED를 확인했습니다. 같은 실행을 재시작하지 않았고 새 분류 제출 검증의 성공 증거로 사용하지 않습니다.

P4 역할 입력 실패의 전송 구간을 대조했습니다. 10:49:23.077 UTC game.observe는 Unity 큐 60.7ms·처리 23.8ms, HTTP 헤더 대기 1,819.6ms·본문 읽기 5.6ms였습니다. 정상 heartbeat 시작 22.844 뒤 다음 시작은 24.178로 약 1,334ms 벌어졌고 해당 요청은 타임아웃됐습니다. JSON 본문 읽기 비용만으로 이 지연을 설명할 수 없습니다. 종료 후 시스템 메모리 여유율 64%, 디스크 가용 약 12GiB(사용률 98%)를 확인했지만 실패 시 동시 측정이 아니므로 원인으로 단정하지 않습니다. Connection: close는 기존 비교 실험에 근거해 유지돼 있어 근거 없이 제거하지 않았습니다.

HTTP 지연 조사에 응답 헤더 기반 진단을 추가했습니다. serializeMs는 JSON·UTF-8 직렬화 시간, resumeMs는 명령 완료부터 응답 처리 재개까지의 시간, serveMs는 Serve 진입부터 응답 쓰기 직전까지의 시간입니다. TCP 연결 수락 전이나 실제 쓰기 완료까지의 지연은 포함하지 않습니다. 정상 수치만 명령 이력에 저장하고 기존 빌드처럼 헤더가 없으면 빈 진단으로 남깁니다. 공유 응답 본문을 수정하지 않습니다. 타입 검사와 직접 C# 컴파일이 통과했으며 실제 빌드·지연 측정 검증은 남아 있습니다.

HTTP 진단 호환성 테스트 1개가 통과했습니다. 정상 진단 값, 헤더가 없는 기존 응답, NaN·음수·빈 헤더에서도 명령 결과가 유지됨을 확인했습니다. 빈 값을 0ms로 오인하지 않도록 수정했고 타입 검사도 통과했습니다. Unity 응답 측정을 포함한 빌드를 시작했으며 실기 확인은 남아 있습니다.

HTTP 진단 빌드는 Editor.log 104811행에서 성공했습니다. 실행 `e8f9e8d4-a8a0-4dc2-9e8c-4399d43ddac3`은 도착 단계를 통과해 네 역할 모두 P_TRIAGE의 후속 퀘스트를 받았으며 첫 환자 접근 중 NAVIGATION_STUCK로 종료됐습니다. 모두 EXITED였습니다. 진단이 포함된 명령 1,154개에서 최대 직렬화 시간 약 272.9ms, 최대 응답 처리 재개 지연 약 133.3ms를 확인했습니다. 이 실행은 앞선 제어권 만료의 원인을 입증하지 않으며, 분류 UI 제출 전의 이동 정체를 다음 조사 대상으로 남깁니다.

입력 기반 이동기는 거리 감소만 진전으로 계산해 제자리 회전이 오래 걸리면 정체로 오판할 수 있었습니다. 목표 방향 오차가 줄어드는 경우도 진전으로 계산하도록 수정했습니다. 회전 진전이 있는 경우는 500ms 정체 한도를 넘는 전체 회전 과정을 완료하고, 회전·이동이 모두 없는 경우에는 여전히 NAVIGATION_STUCK가 발생하는 테스트를 통과했습니다. 타입 검사도 통과했습니다. 전체 이동 제한 시간은 유지합니다. 이 수정이 앞선 실기 정체를 해결했는지는 새 실제 실행으로 확인 중입니다.

실행 `8d803c2c-5502-46bd-92c7-05a42e89703c`에서 patient_b와 patient_c에 정상 상호작용 및 분류 UI 입력을 수행했고, 각 제출 뒤 submitted·correct 신호를 모두 확인했습니다. 세 번째 patient_dummy_d_b 접근 중 NAVIGATION_STUCK가 발생했으며 네 프로세스 모두 EXITED였습니다. 전체 분류 퀘스트 및 시나리오 완료는 아직 입증하지 못했습니다. 이동 대상의 실제 위치에 축별 ±10m 이내 targetOffset을 적용하는 기능과 환자 앞쪽을 거치는 접근 경로를 추가했습니다. 오프셋 도착 및 범위·길이·유한값 검증 테스트 1개와 타입 검사가 통과했습니다. 새 경로의 실기는 진행 중입니다.

앞쪽 접근 경로 실행 `93208865-0c4f-4013-830a-857e4f1e8e71`은 첫 두 환자의 submitted·correct 신호를 다시 확인했고 세 번째 환자의 분류창까지 열었습니다. 이후 triage-level-level5가 클릭 가능한 상태로 확인되지 않아 실패했습니다. 실패 관측의 입력 문맥은 TriageAssessmentUIController였으며 모두 EXITED였습니다. 분류 UI는 고정 폭 항목을 가로로 배치하므로 화면 밖 노출 가능성을 조사해야 하지만, 이 실행의 쿼리 결과 본문이 저장되지 않아 원인을 확정하지 않습니다. 재현 시 해당 UI 쿼리 결과를 로컬 파일에 보존하도록 보완했습니다.

진단 실행 `d2d47746-07c8-46cf-af65-235068490ecb`에서 세 번째 환자의 분류 UI 실패 원인을 확인했습니다. 960×540 화면에서 5등급 중심이 x≈1399, y≈215였고 visible·enabled는 true, interactable은 false, scrollContainerId는 null이었습니다. 모두 EXITED였습니다. 고정 가로 배치가 화면 폭을 초과하는 게임 UI 문제입니다. 분류 패널에 세로 ScrollView와 등급 행 줄바꿈을 추가해 작은 화면에서도 정상 스크롤로 접근하도록 수정했습니다. 직접 C# 컴파일은 통과했고 실제 빌드 및 선택 검증은 남아 있습니다.

분류 UI 수정 빌드는 Editor.log 105876행에서 성공했습니다. 실기 `718fee6a-a69e-4f4f-88c4-a63879addbe7`은 arrival_movement의 P4 DEADLINE_EXCEEDED로 중단됐으며 P1~P3 이동은 fulfilled였습니다. 네 프로세스 모두 EXITED였습니다. 후속 관측에서 아직 P_ANNOUNCE_ARRIVAL이고 복구 기록은 없었으므로 도착 완료로 대체 판정하지 않았습니다. 분류 UI 수정의 실제 선택 검증은 이 실행으로 입증되지 않았습니다. 세 환자 정답 제출 이후 네 역할 모두 무복구 상태로 P_MOVE 및 Quest_Move_BC를 받는지 확인하는 후속 판정도 스크립트에 추가했고 타입 검사가 통과했습니다.

P4 도착 시간 초과 실행의 마지막 명령 기록에서는 입력·관측·heartbeat가 정상 응답했습니다. 마지막 두 입력 응답은 311ms·291ms였고, 위치 관측상 다른 플레이어들은 입구 근처에 모여 있었습니다. 충돌 원인을 확정하지는 않으며, 동일 중심 접근을 도착 구역 내부의 플레이어별 분산 지점 접근으로 변경해 검증합니다. 타입 검사가 통과했습니다.

분산 도착 지점 실행 `2803994c-53fb-46c9-ba13-315fdcdc47fb`은 P1·P3·P4 STALE_CONTROL_EPOCH와 P2 관측/작업 시간 초과로 arrival_movement에서 실패했습니다. 모두 EXITED였습니다. 여러 클라이언트의 입력·관측·heartbeat 지연이 함께 관측됐고, 별도 로컬 파일 조회 프로세스도 약 10초 안에 완료되지 않았습니다. 공통 자원 지연 가능성은 있으나 원인은 확정하지 않습니다. 경로 및 분류 UI 수정의 성공을 이 실행으로 판정하지 않습니다. Runner 및 UI navigation 테스트 43개가 통과했습니다.

반복 이동 관측의 정적 아이템 전체 검색·직렬화 비용을 줄이기 위해 includeStaticItems 옵션을 추가했습니다. 기본 관측은 기존처럼 목록을 제공하며, 정적 아이템 이외 대상을 향한 이동에서만 목록을 null로 생략합니다. 정적 아이템 대상으로 이동할 때 최신 목록을 계속 요청하는 테스트 1개, 타입 검사, 직접 C# 컴파일 및 변경 형식 검사가 통과했습니다. 실제 응답 지연 감소와 4인 제어권 유지에 미치는 효과는 아직 측정하지 않았습니다. 새 Unity 빌드 반영도 남아 있습니다.

관측 옵션 빌드는 Editor.log 106940행에서 성공했습니다. 실기 `66ef6bf5-9afe-4ba0-893a-7f1dfda6d1aa`는 join 단계 DEADLINE_EXCEEDED로 종료됐으며 네 프로세스 모두 EXITED였습니다. 관측 비교 단계에 도달하지 않아 응답 크기 및 지연 감소는 미검증입니다. 종료 후 시스템 부하는 277.04·156.18·74.94, 메모리 free 표시는 64%, 디스크 가용 공간은 12GiB였습니다. 프로세스 자원 조회에서는 Unity Editor가 CPU 94.3%로 가장 높았습니다. 실패 시점과 동일한 측정이 아니므로 원인으로 단정하지 않습니다. 이번 실행을 전체 퀘스트 또는 분류 UI 수정 통과 증거로 사용하지 않습니다.

접속 실패 `66ef6bf5-9afe-4ba0-893a-7f1dfda6d1aa`의 실패 단계는 client_spawned/P4였습니다. 종료 직전 후속 관측에서는 네 역할 모두 localPlayerReady:true, 호스트 연결 수 4를 확인했습니다. 제한 시간 내 준비 확인 실패이며 영구 접속 불가로 단정하지 않습니다. 접속 대기의 반복 관측에서도 정적 아이템 목록을 생략하도록 변경했고 최종 성공 스냅샷·실패 증거는 전체 관측을 유지합니다. 기존 접속 테스트 4개와 타입 검사가 통과했습니다.

실기 `7e6ea242-6746-4671-b5da-bdbbad78e432`은 접속 및 관측 비교를 통과했습니다. 기본 관측의 정적 아이템은 311개였고 생략 요청에서는 null이었으며 로컬 플레이어 관측을 유지했습니다. 응답 크기는 59,551→5,272바이트로 약 91% 감소했습니다. 이는 지연 감소나 전체 안정성 증거를 대체하지 않습니다. 이후 P1·P4 이동 정체가 있었고 P1~P3 도착 퀘스트는 completed:true, P4는 false였습니다. arrival_quest_confirmation의 P4 DEADLINE_EXCEEDED로 종료됐으며 네 역할 모두 P_ANNOUNCE_ARRIVAL, 복구 기록 없음, 전체 EXITED였습니다. 분류 UI 및 전체 퀘스트 완료는 아직 미검증입니다.

`7e6ea242-6746-4671-b5da-bdbbad78e432`의 P1·P4 최종 위치는 각각 x≈−74.32/z≈−4.76, x≈−69.89/z≈−4.69였고 가운데로 접근한 P2·P3는 z≈−0.89/−1.14에 도달했습니다. 좌우 경로 정체를 해결하기 위해 B·C 도착 절차를 중앙 경유 후 내부 분산으로 바꾸고, 같은 경유 지점에서 플레이어끼리 막히지 않도록 차례로 이동하게 했습니다. 실제 완료 조건과 무복구 검증은 유지합니다. 타입 검사가 통과했고 실기를 진행 중입니다.

중앙 경유 실행 `eefb753e-11ac-4d10-b68e-34ff9821db6e`에서 네 역할의 arrival-movement 및 arrival-confirmation이 모두 fulfilled였고 후속 분류 단계까지 진입했습니다. 수정한 분류 UI에서 첫 patient_b의 제출·정답 신호와 이후 관측을 저장했습니다. 두 번째 환자의 분류 전 triage_submission 단계에서 STALE_CONTROL_EPOCH가 발생했으며, 마지막 호스트 상태는 Gameplay/P_TRIAGE, 복구 기록 없음이었습니다. 모두 EXITED였습니다. 중앙 경유 도착 절차의 성공을 확인했지만 5등급 선택 및 전체 분류 퀘스트 완료는 아직 미검증입니다.

`eefb753e-11ac-4d10-b68e-34ff9821db6e`의 첫 제어권 만료 직전, 1초 제한 heartbeat 두 개의 응답 처리가 각각 약 6,959ms·6,117ms 뒤에 끝났고 같은 시점 관측은 7,245ms였습니다. 해당 관측의 Unity 처리 시간은 약 19.9ms, Serve 측정은 약 868.9ms여서 전체 지연을 Unity 명령 처리만으로 설명할 수 없습니다. 에디터 최소화 비교 실행 `40ffb91a-dc94-4005-83de-32d440292f56`도 scenario_start에서 CANCELLED로 중단됐고 모두 EXITED였습니다. 최소화의 해결 효과는 입증되지 않았습니다. 저장은 기존부터 별도 worker였으므로 저장 경로를 추측으로 바꾸지 않았습니다. 환자 실행에 단계별 1초 타이머 간격·이벤트 루프 최대 지연의 최근 300개 표본과 commandHistoryWrites를 남기는 entry-scheduling.json을 추가했습니다. 실제 표본 수집 검증은 다음 실행에 남아 있습니다.

지연 수집 실행 `5bad5bc3-a173-449e-b99c-353fa774c550`은 도착 후 triage_submission의 NAVIGATION_STUCK로 종료됐고 모두 EXITED였습니다. 제출 증거는 없었습니다. 단계별 표본 75개 중 타이머 간격 최대는 약 1,006.4ms였고, 기록 저장 1,150건의 최대 대기는 약 199.2ms였습니다. 이번 실패를 제어권 만료로 분류하지 않습니다. 분류 접근 중앙 지점 주변에 대기 역할이 남아 있어, 대기 역할 세 명을 서로 다른 통로 지점으로 정상 이동시키고 분류 담당자의 경유 지점을 왼쪽으로 옮겼습니다. 변경 후 실기 검증은 남아 있습니다.

실기 `4c9f52f4-5d10-45e6-b7b8-0f9935c1934e`은 정상 종료 코드 0으로 도착 및 세 환자 분류 단계를 통과했습니다. patient_b·patient_c의 2등급, patient_dummy_d_b의 5등급을 정상 상호작용·마우스 입력으로 제출했고 각 submitted·correct 신호를 확인했습니다. 네 역할 모두 P_MOVE에서 patient_b_c_ct의 Quest_Move_BC를 받았고 recoveryNotes는 빈 배열이었습니다. 전체 EXITED를 확인했습니다. 단계별 표본은 126개, 최대 타이머 간격은 약 1,044.0ms였습니다. 이 증거는 수정한 분류 UI의 5등급 접근과 분류 단계 진행을 입증하며, 이후 침대 이동·처치·CT 이송 또는 전체 시나리오 완료를 입증하지 않습니다. fullPlayPassed:false를 유지합니다.

후속 침대 이동 검증을 위해 읽기 전용 vehicles 관측을 추가했습니다. 실제 위치·전진 방향 기준 yaw·로컬 조종 여부와 침대의 정렬 지점 및 씬 목표 좌표를 제공합니다. 일반 빌드에서는 UNITY_E2E/UNITY_EDITOR 조건으로 제외합니다. 직접 C# 컴파일이 통과했습니다. drivePatientBed 도우미는 A/D 조향과 W 전진 입력을 사용하고, 목표 근접만으로 완료 처리하지 않으며 실제 latchedPointId를 확인합니다. 조종권 없음·중복 대상에서 입력을 보내지 않고 종료 시 입력을 해제하는 테스트를 포함해 2개 테스트와 타입 검사가 통과했습니다. Unity 새 빌드 반영·실제 침대 이동·시나리오 스크립트 연결은 남아 있습니다.

침대 이동 도우미를 live-patient-entry.ts의 move 단계에 연결했습니다. 기존 도착·분류 후 bed_b와 bed_c에 정상 접근·move_bed 상호작용을 수행하고, 로컬 조종 상태를 기다린 뒤 각각 zone_0:bed_snap_point·zone_1:bed_snap_point를 향해 조향·전진합니다. 각 실제 정렬 후 증거를 저장하고, 네 역할 모두 무복구 상태로 P_MOVE를 벗어나 같은 시나리오의 후속 퀘스트를 받는지 확인합니다. vehicle 이동 대상은 관측된 식별자로 조회합니다. 타입 검사 및 도우미 테스트 2개가 통과했습니다. 침대 관측을 포함한 Unity 빌드를 시작했으며 실제 이동 검증은 남아 있습니다.

침대 관측 빌드는 Editor.log 108010행에서 성공했고 빌드 어셈블리의 AutomationState 포함을 확인했습니다. 실기 `e74cc5a2-3b90-4ecc-99a1-8c13dfb04686`은 scenario_start에서 CANCELLED로 종료됐고 모두 EXITED였습니다. 실제 vehicles 관측에 bed_b·bed_c와 각 정렬 목표 5개가 나타났습니다. 최대 1초 표본 간격은 약 1,004.9ms였으나 Return의 Unity 처리 약 1,623ms 동안 heartbeat 두 개가 시간 초과되고 제어권이 만료됐습니다. 2초 watchdog은 유지하고 시나리오 시작 Return 직전에 기존 epoch의 heartbeat를 명시적으로 완료하도록 추가했습니다. 만료된 제어권 재획득은 하지 않으며 이 보완의 실기 검증은 남아 있습니다.

시작 직전 갱신을 포함한 실행 `89f28b49-8091-4cc8-b26b-0b9e81802bd9`은 시작·도착을 통과했지만 triage_waiting_positions에서 P2 작업 시간 초과로 종료됐고 모두 EXITED였습니다. 같은 단계의 타이머 간격 최대는 약 3,228ms였고 P2 관측 약 3,298ms·heartbeat 약 2,633ms 시간 초과 후 STALE_CONTROL_EPOCH가 기록됐습니다. 경로 결함으로 단정하지 않고 기존 이동 제한을 유지했습니다. 실패 증거에 최근 30초 heartbeat 오류 최대 24개를 추가해 원래 오류·단계와 함께 보존하도록 변경했습니다. 침대 조종 실기는 아직 도달하지 못했습니다.

그래픽 방식 비교를 위해 실행 스크립트의 다섯 번째 인자로 빌드 프로필을 선택하고 role-entry에 기록하도록 확장했습니다. 기본값은 mac_direct입니다. 기본 그래픽 mac 프로필의 실기 `f3f84f27-03dd-4a07-bdd0-088ed12b73ca`도 triage_waiting_positions의 STALE_CONTROL_EPOCH로 종료됐고 모두 EXITED였습니다. 해당 단계의 최대 타이머 간격은 약 2,359.5ms였습니다. 따라서 force-gfx-direct만의 문제라고 판정하지 않습니다. 타입 검사가 통과했습니다. 기존 MppmLiteMode는 추가 Editor 전용이므로 일반 빌드 비교에는 그대로 적용할 수 없음을 소스에서 확인했습니다.

빌드 프로필에 frameRateLimit 선택값을 추가하고 기본 30fps를 유지했습니다. 15fps 비교 실행 `e157e03b-4c50-4764-b307-f1fef8197dcc`의 실제 관측에서 targetFrameRate:15를 확인했습니다. 초기 fps는 약 6.5였습니다. 이후 triage_waiting_positions에서 작업 시간 초과로 중단됐고 모두 EXITED였습니다. 프레임 제한 감소만으로 안정성 개선을 입증하지 못했으며 기본 설정은 바꾸지 않았습니다. 타입 검사가 통과했습니다.

초기 load 6.33에서 재실행한 `984a4ab3-d94c-43d7-832d-b3310f9aba7b`도 triage_waiting_positions에서 CONTROL_DENIED로 종료됐고 모두 EXITED였습니다. 최근 heartbeat 실패 10개가 저장됐고 최대 타이머 간격은 약 2,182ms, 해당 eventLoopMax는 약 1,344ms였습니다. 종료 후 load는 36.16이었습니다. 열·전력 제한 조회는 OS에서 실패해 확인하지 못했습니다. 다음 비교를 위해 빌드별 renderScale(0.25~1, 기본 0.75) 선택값과 mac_direct_half_render(0.5) 프로필을 추가했습니다. UI 해상도와 기본 30fps를 유지하며 실제 효과 검증은 남아 있습니다. 타입 검사가 통과했습니다.

렌더 비율 0.5 비교 `d8f7bcb1-b223-4e6d-9f00-691e46015c98`은 arrival_movement의 STALE_CONTROL_EPOCH로 종료됐고 모두 EXITED였습니다. 최근 heartbeat 오류는 10개, 최대 타이머 간격은 약 4,426ms였습니다. 같은 프로젝트 에디터 PID 71264를 SIGSTOP으로 잠시 정지한 비교 `54cac744-720f-446e-90c2-c2de6af292f4`도 scenario_start에서 CANCELLED로 종료됐으며 모두 EXITED였습니다. 래퍼 finally에서 SIGCONT를 실행해 에디터를 재개했습니다. 이 실행의 최대 타이머 간격은 약 1,023ms였습니다. 렌더 비율 감소나 에디터 정지가 해결책임을 입증하지 못했습니다.

원문 명세 17.1에서 heartbeat 500ms·입력 임대 2초는 예시값임을 확인했습니다. 8.7의 유지 입력 유효 기간·취소/연결 종료 시 해제·메인 스레드 복구 시 오래된 입력 우선 해제는 유지해야 합니다. 빌드 프로필의 controlLeaseMs(2,000~10,000, 기본 2,000)를 환경 변수로 전달하고 실제 control 관측에 반영하도록 추가했습니다. 키 입력의 기존 최대 2초 유효 기간과 명령 TTL은 변경하지 않았습니다. 타입 검사와 직접 C# 컴파일이 통과했습니다. 새 빌드 및 임대 변경 시 입력 해제·마우스 버튼·중단 회귀 검증은 남아 있으므로 긴 임대 조건의 통과 증거는 아직 없습니다.

제어권 임대와 독립적으로 마우스 버튼의 마지막 누름/이동 입력 뒤 2초가 지나면 해제되도록 추가했습니다. 해제 경로는 새 장치를 생성하지 않고 기존 마우스에 버튼 해제 이벤트를 큐에 넣습니다. Bridge Update에서 명령 처리 전에 만료를 확인합니다. 직접 C# 컴파일이 통과했고 Unity EditMode 입력 테스트 16개가 모두 통과했습니다(2026-09-08 12:18:43~44 UTC). 신규 두 테스트는 만료 전/후 요청 상태 및 장치 미생성, 실제 Input System 버튼 해제를 검증합니다. 새 플레이어 빌드와 긴 제어권 임대에서의 실행 중단 실기는 남아 있습니다.

입력 독립 만료 빌드는 Editor.log 109203행에서 성공했습니다. 실기 `b431d29c-e699-4951-a0a0-e3008f2d32c4`는 5초 제어권 임대를 확인한 뒤 W 키와 실제 가상 마우스 버튼을 눌렀습니다. heartbeat와 같은 epoch의 Automation 소유권이 유지되는 동안 2.5초 뒤 키·요청 버튼·실제 버튼이 해제됐습니다. heartbeat 중단 뒤 5.5초 대기 후 owner:None과 epoch 증가, 새 입력 거부를 확인했습니다. 종료 코드 0 및 EXITED였습니다. 이 결과는 5초 임대의 입력 해제 동작을 입증하며 4인 안정성이나 환자 전체 퀘스트 완료를 대체하지 않습니다. mac_direct_5s_lease 비교 프로필을 추가했고 기본 2초 프로필은 유지했습니다.

5초 임대 4인 실행 `420561a1-22ce-417b-93e8-3a8c19f6b285`는 join의 작업 시간 초과로 종료됐고 모두 EXITED였습니다. 최대 타이머 간격은 약 6,612ms, 최근 heartbeat 오류는 9개였습니다. 임대 증가만으로 4인 안정성을 입증하지 못했습니다. 다른 실행 장비 사용 가능 여부를 사용자에게 질문했고 응답은 아직 없습니다. 별도로 침대 관측의 requiredInteractors:4를 확인해 이동 도우미에 참여자 목록을 추가했습니다. 모든 참여자의 조종 상태를 확인하고 같은 조향 입력을 전송하며, 한 명의 실패 시 진행 중인 동료 입력 종료를 기다린 후 모두 해제합니다. 관련 테스트 4개와 타입 검사가 통과했습니다. 실제 네 역할의 침대 탑승 연결과 협동 이동 실기는 남아 있습니다.

move 단계에 네 역할의 순차 접근·move_bed 상호작용·로컬 조종 확인을 연결했습니다. 각 탑승 증거를 boarded-{bed}-{actor}.json에 저장하고, 네 명 모두 확인된 뒤 참여자 전체에 협동 조향·전진을 보냅니다. 단계 이름에 침대와 역할을 포함합니다. 조종 도우미 테스트는 네 참여자의 동일 조향 및 정렬 후 전체 해제를 추가해 5개가 통과했고 타입 검사도 통과했습니다. 이는 모의 제어 테스트이며 실제 네 명 탑승·협동 이동·전체 퀘스트 완료는 아직 입증하지 못했습니다.

4인 협동 탑승을 연결한 실행 `78c3450e-b0d7-4667-87d4-7b5d0b77e72a`는 네 역할 모두 도착 단계를 통과했으나 triage_submission에서 작업 시간 초과로 종료됐습니다. 분류 제출 및 탑승 완료 파일은 생성되지 않았습니다. 최대 타이머 간격은 약 5,807ms, 해당 eventLoopMax는 약 4,907ms였고 최근 제어 실패 24개를 기록했습니다. 모든 프로세스는 EXITED입니다. 전체 퀘스트 완료나 협동 침대 이동의 통과 증거로 사용하지 않습니다.

QuestManager의 실제 완료 통지 경로에 읽기 전용 quest.completed 이벤트를 추가했습니다. 런타임 퀘스트 ID·정의 ID·소스 시나리오·완료 여부·대기 자리 표시 여부·범위를 기록합니다. UNITY_E2E/UNITY_EDITOR 조건으로 한정하며 게임의 완료 판정은 변경하지 않습니다. 직접 C# 컴파일과 diff 공백 검사가 통과했습니다. 새 빌드에서 이벤트 수집을 확인하는 실기와 역할별 전체 완료 판정 연결은 아직 남아 있습니다. 이 이벤트만으로 정상 플레이나 최종 종료를 입증하지 않습니다.

실기 스크립트의 종료 경로에 인스턴스별 quest-evidence 파일과 export 결과를 연결했습니다. 실패 실행에서도 저장된 이력을 조회하며 내보내기 오류가 나더라도 finally에서 플랫폼을 종료합니다. 이벤트의 runId·instanceId·scenarioId가 모두 일치하고 completed:true·placeholder:false인 완료만 목록에 포함합니다. 반복 완료의 개별 증거와 시나리오 시작/종료 이력도 보존합니다. 이 파일은 완료 증거 색인이며 fullPlayPassed:false를 유지합니다. 다른 실행/플레이어/시나리오·미완료·대기 퀘스트 제외 및 반복/종료 이벤트 검증 테스트 2개와 타입 검사가 통과했습니다. 실제 새 빌드의 이벤트 수집은 미검증입니다.

퀘스트 완료 관측을 포함한 macOS 자동화 빌드가 Editor.log 110320행에서 성공했습니다. 완료 이벤트의 실제 수집 및 환자 후속 단계 검증은 아직 남아 있습니다.

새 완료 이벤트 빌드의 실행 `e99d551f-09db-4520-8bfc-a94594a3a793`는 join 단계에서 DEADLINE_EXCEEDED로 종료됐습니다. 네 인스턴스의 완료 증거 내보내기도 HISTORY_STORAGE_TIMEOUT으로 실패했습니다. 프로세스 종료 루틴 뒤 저장소 close 오류가 전파되어 entry-shutdown 및 scheduling 보고서 작성이 생략되는 문제를 확인했습니다. 이력 종료 오류를 cleanupError로 기록하고 원래 오류를 보존해 후속 보고서 작성을 계속하도록 수정했습니다. 타입 검사가 통과했습니다. 이번 실행으로 실제 퀘스트 완료 이벤트 수집은 입증하지 못했습니다.

이력 저장 시간 초과 조사에서 DB 크기는 약 612.2MiB였고 EXPLAIN QUERY PLAN상 중복 확인은 events_run_sequence, 이력 조회는 events_run_id 인덱스를 사용했습니다. 전체 검색이 원인이라는 증거는 없습니다. 협동 침대 이동의 동료 관측을 Promise.allSettled 병렬 조회로 변경했습니다. 모든 관측이 종료된 뒤 오류를 전파하고, 유효한 patientBed 조종 상태를 확인한 경우에만 입력을 보냅니다. 관측 실패 시 동료 관측 종료 전 해제하지 않는 회귀를 포함해 도우미 테스트 6개 및 타입 검사가 통과했습니다. 실제 이동 지연 감소나 4인 안정성 개선을 입증한 결과는 아닙니다.

소스 확인에서 침대 이동 후 care_patient_b → DOC_C → DOC_D → P_B_CARE 전환을 확인했습니다. 기존 이동 검증은 후속 대화를 진행하지 않고 퀘스트를 기다렸습니다. DOC_C/DOC_D의 정상 대화 입력을 연결하고 예상하지 않은 그래프·대사·선택지는 실패 처리합니다. 후속 퀘스트 판정도 P_B_CARE 및 각 역할의 Quest_B_Recognition/Vital/Pupil_IV/Oxygen_Bleeding을 요구하도록 구체화했습니다. 타입 검사가 통과했습니다. 이 전환과 실제 처치·CT 완료의 실기 검증은 남아 있습니다.

/a~ /d 정상 채팅 뒤 역할 배정 관측을 확인하는 단계를 추가했습니다. 네 인스턴스의 로컬 플레이어 ID가 서로 달라야 하며, 모든 클라이언트가 같은 네 플레이어에게 nurse_a/b/c/d가 각각 하나씩 배정됐다고 관측해야 합니다. 불일치는 최대 15초 동안 재관측하며 실제 제어/관측 오류는 숨기지 않습니다. 역할 중복·누락·사용자 ID 중복·클라이언트 간 불일치 검증 테스트 1개와 타입 검사가 통과했습니다. 기존 실기 4c9f52f4-5d10-45e6-b7b8-0f9935c1934e의 entry-ready-p1~p4 관측을 새 검사에 넣어 네 역할 배정을 확인했습니다. 이는 해당 과거 실행의 역할 배정 증거이며 새 실행이나 전체 퀘스트 완료를 입증하지 않습니다.

최근 변경을 포함한 전체 TypeScript 회귀 테스트를 node --test --test-timeout=60000 test/*.test.ts로 실행해 142개 전부 통과했습니다(실패·취소·스킵 0개, 약 15.1초). 결과는 artifacts/unity-e2e/suite-142.log에 저장했습니다. 최초 샌드박스 실행은 localhost 관련 항목들이 실패하고 종료되지 않아 해당 로그를 연 npm/node 및 확인된 자식만 종료했습니다. localhost 권한을 적용한 실행은 종료 코드 0입니다. 이 결과는 모의 서버·도구 회귀 검증이며 실제 Unity 4인 시나리오 전체 완료를 대체하지 않습니다.

A 시나리오 도착 검증 실행 `6a86baeb-a410-4493-82e0-e04354573cb6`는 입장 이후 role_commands의 STALE_CONTROL_EPOCH로 종료됐습니다. 최근 제어 실패 15개가 기록됐고 네 프로세스 모두 EXITED이며 cleanupError는 없습니다. 네 인스턴스의 quest-evidence 내보내기는 모두 성공했지만 시나리오 시작 전 실패했으므로 환자 퀘스트 완료를 입증하지 않습니다. 종료 보고서와 타이밍 보고서는 정상 저장됐습니다.

반복 지연 조사로 동일 프로젝트 에디터 PID 71264의 2초 CPU 스택을 수집했습니다. 메인 스레드 표본 157개 중 156개가 이벤트 대기 경로였고, 자동화 코드의 지속 CPU 점유를 입증하지 못했습니다. 해당 시점 physical footprint는 약 7.1GB였습니다. 파일은 artifacts/unity-e2e/editor-load-sample-20260908.txt에 보관했습니다. 기존 monitorMemoryPressure를 환자 실기 스크립트에 연결해 10초 간격 메모리 압력 및 조회 소요 시간을 entry-memory-pressure.json에 저장합니다. 이는 부하 원인 판정을 위한 관측이며 메모리가 기존 실패의 원인이라는 결론은 아닙니다.

메모리 관측을 포함한 실행 `6a893f42-a96f-40a5-b11f-6fdc8bb948e6`에서 네 역할 일치·기본 진입·도착을 통과했습니다. 네 인스턴스 모두 실제 quest.completed 이벤트로 Quest_Arrive_Triage를 기록해 새 관측의 실기 수집을 확인했습니다. B/C 환자 분류 제출은 통과했으나 세 번째 환자 접근 중 input.execute가 시간 초과됐습니다. 최근 heartbeat 오류는 0개, 최대 타이머 간격은 약 1,354ms였습니다. 메모리 압력 조회의 systemWideFreePercent는 시작 66%에서 플레이 중 30~33%였으며 조회는 4~8ms였습니다. 메모리 부족이나 제어권 만료가 이번 실패 원인이라고 판정할 근거는 없습니다. 모든 프로세스는 EXITED이며 전체 분류·협동 이동·전체 퀘스트 완료는 미검증입니다.

6a893f42의 마지막 이동 입력은 약 2,254ms 뒤 INSTANCE_UNAVAILABLE/TimeoutError로 기록됐습니다. 호출자 전체 제한 만료라면 CANCELLED가 기록되는 코드이므로 해당 실패는 명령별 응답 대기 제한에 해당합니다. 직전 입력들은 약 128~559ms였습니다. Runner.navigate가 일반 명령 기본 3초보다 짧은 TTL 2초를 별도로 적용하고 있어 이를 3초로 맞췄습니다. 정상 이동 입력은 100ms이고 Unity 키 입력 최대 2초 만료, 실패 명령 자동 재전송 금지는 유지합니다. Runner 테스트 40개와 타입 검사가 통과했습니다. 실제 개선 여부는 다음 실기에서 확인해야 합니다.

3초 이동 TTL 실기 `29f229c8-270b-499a-b29b-6222ac219997`는 네 역할 배정·도착·세 환자 분류 및 모든 역할의 Quest_Move_BC 전환을 통과했습니다. bed_b에 p1/p2/p3가 정상 탑승해 로컬 조종 상태를 확인했습니다. p4는 남쪽 대기 위치에서 공통 접근 지점으로 가던 중 [-74.44682,0.09,-4.70949]에서 NAVIGATION_STUCK으로 실패했습니다. heartbeat 오류는 0개, 최대 타이머 간격은 약 1,096ms이며 모두 EXITED입니다. 침대는 이동 전 [-74,0,2.3]이었습니다. p4가 남쪽에서 대각선으로 접근하는 대신 도착 지점 기준 [0,0,-3]을 먼저 경유한 뒤 기존 접근 지점으로 이동하도록 정상 입력 경로를 추가했습니다. 정확한 충돌 오브젝트 및 경로 수정 효과는 아직 미검증입니다.

경유 경로 실기 `cb3afbb1-2144-4b45-8ee0-e2095c34480d`는 세 환자 분류 및 네 역할의 bed_b 정상 탑승을 통과했습니다. 네 탑승 관측 파일과 before-move 증거가 생성됐습니다. 협동 이동에서 침대가 [-74,0,2.3]에서 [-70.61402,0,-3.9257853]까지 움직였으나 목표 정렬 전 실패했습니다. heartbeat 오류는 0개이며 모두 EXITED입니다. 기존 오류 문자열은 Error: bed_b로 오류 코드를 잃고 있어 후속 entry-error에 errorCode 필드를 추가했습니다. 목표 latching 및 전체 퀘스트 완료는 미검증입니다.

cb3afbb1 호스트 unity.log에서 실제 이동 차단 콜라이더 TriageArea/MainDoor(TriageZone) (2)/WallSkirting을 확인했습니다. boundsCenter [-68.69,0.03,-4.03], boundsSize [4,0.19,0.20], hitPoint [-70.61,0,-3.93]입니다. 협동 이동 도우미에 입력 기반 경유 좌표를 지원하고 B/C 침대 경로에 [-72.7,0,-2],[-72.7,0,-6]을 추가했습니다. 경유 도달 시 진행 측정을 초기화하되 최종 통과는 기존 실제 latchedPointId를 요구합니다. 경유/정렬 및 잘못된 좌표 검증을 포함한 테스트 7개와 타입 검사가 통과했습니다. 실제 출입구 통과와 목표 정렬은 아직 미검증입니다.

출입구 경유 실기 `63e2bd19-611b-4879-8d69-f7b282fe6f39`는 네 역할의 탑승 후 기존 WallSkirting 장애물을 통과했습니다. 침대가 [-66.69901,0,-10.7263994]까지 이동했으며 이번 NAVIGATION_STUCK 원인은 TreatmentRoom/autoDoor_1/sm_autoDoor_1 콜라이더였습니다. 로그상 boundsCenter [-66.68,1.12,-10.79], boundsSize [0.05,2.28,1.18]입니다. heartbeat 오류는 없고 모두 EXITED입니다. 자동문 통과 조건과 목표 정렬은 아직 미검증입니다.

자동문 원본 스크립트는 OnTriggerEnter/Exit로 개폐하며, 침대 탑승은 플레이어 SetForcedFollowAnchor로 연결됨을 확인했습니다. 탑승 상태에서 트리거 반응이 보행과 같은지는 미검증입니다. Bridge의 읽기 전용 automaticDoors 관측에 문 위치, 실제 트리거 bounds 중심/크기/활성 여부, 이동 패널의 월드·로컬 위치를 추가했습니다. 서드파티 자동문 코드나 문 상태는 수정하지 않습니다. 직접 C# 컴파일 및 변경 파일 공백 검사가 통과했습니다. 새 빌드와 실제 문 관측·통과 검증은 남아 있습니다.

자동문 관측 빌드 준비 중 에디터 UI의 Assets 메뉴가 전부 비활성 상태로 관측됐고 localhost editor.observe는 Connection refused였습니다. Unity PID 71264는 생존했으나 bee_backend 빌드 프로세스는 확인되지 않았습니다. CUA 앱 연결도 timeoutReached로 실패했습니다. 따라서 새 자동문 관측 빌드를 시작했다고 보지 않으며 이전 빌드를 새 소스 검증에 사용하지 않습니다. 강제 종료·씬 변경은 하지 않았습니다.

에디터 재관측에서 외부 수정된 OverworldScene을 다시 불러올지 묻는 대화상자를 확인했습니다. Reload로 저장된 최신 씬을 반영한 뒤 자동화 빌드를 실행했습니다. 현재 에디터가 기록 중인 Editor-prev.log 123292행에서 E2E build completed를 확인했고, 22:30:36 갱신된 빌드 Assembly-CSharp.dll에 automaticDoors 항목이 포함돼 있습니다. Editor.log는 다른 실행으로 교체되어 최신 에디터 진행을 반영하지 않았습니다. 자동문 실기 관측과 통과 검증은 남아 있습니다.

자동문 관측 실기 `51f6ad68-87fc-49d2-9ce6-4ed9f223d1ef`는 네 역할 탑승 후 침대 B가 [-66.6998,0,-11.25705]에서 NAVIGATION_STUCK으로 멈췄습니다. 자동문 패널의 localPosition은 [1,0,0]으로 프리팹 doorsOpenPosition과 같아 문이 열린 상태임을 확인했습니다. 따라서 트리거 미반응으로 판정하지 않습니다. 문 중심은 [-66.65765,-0.024,-10.182999]이며 대각선 경로가 열린 문짝 쪽으로 치우쳤습니다. 기존 경유 경로 뒤 [-68.3,0,-10.18],[-65,0,-10.18]을 추가해 문 중앙을 직각으로 통과하도록 수정했습니다. 모든 프로세스는 EXITED입니다. 경로 수정 효과 및 최종 정렬은 아직 미검증입니다.

문 중앙 경로 실행 `776b6320-88db-40b8-b0f4-872ed2585fab`은 네 번째 플레이어의 bed_b 탑승 준비에서 INSTANCE_UNAVAILABLE/TimeoutError로 중단됐습니다. 최근 heartbeat 오류 7개와 최대 타이머 간격 약 3,778ms가 기록됐습니다. 마지막 메모리 압력 관측의 systemWideFreePercent는 32~34%였습니다. p1/p2/p3의 탑승 관측은 있으나 수정한 문 통과 경로는 실행되지 않아 효과를 판단할 수 없습니다. 모든 프로세스는 EXITED입니다.

문 중앙 경로 재검증 실행 `5fc6b503-e042-42a7-aac7-eb38b9a1eafe`도 실제 침대 경로에 도달하기 전에 실패했습니다. 종료 및 타이밍 증거를 저장했으며 수정한 경로의 성공/실패 증거로 사용하지 않습니다.

5fc6b503 도착 실패에서는 p1만 이동 제한 시간이 만료됐고 p2/p3/p4는 이동을 완료했습니다. p1의 최종 위치는 [-72.58341,0.09,-2.72879]였습니다. 매 관측 사이 고정 100ms 전진하던 도우미를 보완해 방향 오차 10도 미만에서는 최대 250ms 전진하고, 도착 반경까지 남은 거리/보행 속도로 더 짧게 제한합니다. 방향 오차 10~25도는 기존 최대 100ms입니다. 위치 변경·속도 변경·재전송은 사용하지 않습니다. 거리·속도·방향에 따른 입력 길이 검증을 포함한 Runner 테스트 41개와 타입 검사가 통과했습니다. 실제 도착 안정성 및 문 통과 개선은 미검증입니다.

적응형 보행 실기 `b9f457f3-882d-4cd7-bdc5-6e85b8e377c4`는 도착·세 환자 분류·네 역할의 침대 B 탑승을 통과했습니다. 이동 중 [-72.64359,0,-5.41219425]에서 CANCELLED로 종료됐으며 충돌 로그는 없었습니다. 탑승부터 공유하던 AbortSignal 90초가 만료돼 이동의 자체 60초보다 먼저 취소되는 구조를 확인했습니다. 탑승 90초와 실제 이동 시작부터 60초 제한을 분리했습니다. 입력 TTL 및 키 만료는 그대로입니다. 모든 프로세스는 EXITED입니다. 목표 정렬은 아직 미검증입니다.

시간 제한 분리 후 실기 `7fd6aff7-55fc-4a99-953b-890952ad0be0`는 역할 배정 확인 후 arrival_movement의 INSTANCE_UNAVAILABLE/TimeoutError로 종료됐습니다. 최근 heartbeat 오류 12개, 최대 타이머 간격 약 4,132ms였으며 모두 EXITED입니다. 탑승·이동 단계에 도달하지 못해 제한 분리 및 문 중앙 경로의 효과는 미검증입니다.

분산 실행 가능성 점검에서 config에 실행 노드 항목이 없고 Platform.launch/attach 모두 nodeId:local 및 localhost 브리지를 사용함을 확인했습니다. HTTPS 콘솔 접속과 분산 프로세스 실행을 구분해 README/COVERAGE를 수정했습니다. 원격 실행 노드 기능은 검증만 남은 것이 아니라 구현도 남아 있습니다. 다른 장비 사용 가능 여부에 대한 사용자 응답은 아직 없습니다.

실기 `84ab97f0-6a55-474d-a643-167ae93e3417`은 도착 후 triage_waiting_positions의 NAVIGATION_STUCK으로 실패했습니다. p4는 목표를 지나친 [-72.5258,0.09,-8.514846]에서 회전 중이었고 마지막 입력들은 lookDelta였습니다. 회전 진행 판정이 과거 최소 오차를 유지해, 목표를 지나친 후 되돌아서는 정상 회전도 정체로 판단할 수 있음을 확인했습니다. 직전 관측의 각도 오차와 비교하도록 수정했습니다. 전체 이동 제한 시간은 유지합니다. 모두 EXITED입니다.

회전 수정 실기 `7e52815d-baeb-4a30-ba4f-a4386ad05669`는 도착·대기 위치·세 환자 분류·네 역할 탑승을 통과했습니다. 문 중앙 [-66.6456,0,-10.1799469]에서 NAVIGATION_STUCK이 발생했습니다. 실제 차단물은 TreatmentRoom/autoDoor_1/UCX_auto_door_frame_1 MeshCollider이며 hitPoint [-66.64,0,-10.18], boundsCenter [-66.64,1.20,-10.18], boundsSize [0.10,2.45,2.51]입니다. 열린 문짝을 피하더라도 바닥 높이의 문틀에 걸리는 재현 증거입니다. MinecraftBoatLikeControl은 transform.position 높이에서 단일 레이로 이동을 검사합니다. 원인 확정을 위해 문틀 메시의 바닥 부분과 이동 프로브의 관계를 추가 확인해야 합니다. heartbeat 오류는 0개이고 모두 EXITED입니다. 충돌 무시나 순간 이동으로 통과 처리하지 않았습니다.

사용자 요청에 따라 네 역할 명령과 시나리오 진입 관측을 병렬화했습니다. 실기 `6d0b95b1-6773-4894-aad2-ba107fc924c4`는 네 역할 명령·모든 클라이언트 역할 일치·B/C 기본 진입을 통과하고 종료 코드 0, 네 프로세스 EXITED였습니다. parallel-role-commands.json에 역할별 시작/종료 시각을 남겼습니다. route-manifest.json에는 시나리오/퀘스트/실행 스크립트/보행/침대 제어 소스 SHA-256을 저장합니다. 성공 경로를 결정적인 회귀 테스트로 고정하기 위한 출처 기록이며 전체 완료 전 fullPlayPassed:false를 유지합니다. 좁은 경로의 이동 순서와 동일 침대 접근은 아직 순차적이며, 협동 침대 조작 자체는 기존 네 참여자 병렬 입력입니다.

patient_a_critical 4인 실기 `d9c96d1f-5b29-46e0-ad45-961b96e5d381`은 `/a`~`/d` 역할 배정과 모든 클라이언트의 역할 일치, 도착 퀘스트, 네 명의 bed_a 탑승과 협동 이동, 초기 활력·GCS 사정, 경추 보호대·흡인 처치, 고급 역할 배정, 후두경과 준비된 기관내관 제작, 의사에게 후두경 전달까지 통과했습니다. `patient-a-laryngoscope-submission-events.json`에서 정상 입력으로 발생한 `pass_laryngoscope` 신호를 확인했습니다. 네 프로세스는 모두 EXITED이며 종료 정리 오류는 없습니다. 다만 배치 스크립트가 이 체크포인트 뒤의 기관내관 전달·삽관, 산소·출혈·IV, 심정지와 ROSC 후속 퀘스트를 아직 구현하지 않았으므로 전체 완료 증거가 아닙니다. 기존 스크립트가 이 지점에서 종료 코드 0을 반환하던 문제를 수정하여, `full` 실행은 `patient-a-full-route-checkpoint.json`을 저장한 뒤 `PATIENT_A_FULL_ROUTE_INCOMPLETE_AFTER_FIRST_INTUBATION_SUBMISSION`으로 실패하도록 했습니다. 전체 경로 구현과 재실기는 남아 있습니다.

확장 경로 실기 `8907a56a-8bb9-4efe-944e-27750483212d`는 위 구간을 처음부터 재현한 뒤 준비된 기관내관 제출과 환자의 스타일렛 제거까지 통과했습니다. `patient-a-after-stylet-removal.json`에서 Quest_Intubation_PatientA 진행도 3/5와 앞의 세 작업 완료를 확인했으며, 다음 정상 상호작용으로 의사의 `patient-a-doctor-submit-5cc-syringe`가 노출됐습니다. 배치 스크립트는 이 성공 구간을 포함하고 `patient-a-full-route-checkpoint.json`에 `stylet_removal`을 기록한 뒤 의도한 `PATIENT_A_FULL_ROUTE_INCOMPLETE_AFTER_STYLET_REMOVAL`로 실패합니다. 최근 제어 실패와 종료 정리 오류는 없고 네 프로세스는 모두 EXITED입니다. 5cc 주사기 전달 이후 퀘스트는 여전히 미구현·미검증입니다.

기관삽관 5/5 확장 실기 `b902d94a-b881-42a7-a57d-ab5339ea391b`는 기존 경로와 5cc 주사기 획득·전달까지 통과해 Quest_Intubation_PatientA 4/5를 확인했습니다. 플라스터도 정상 획득했으나 환자 적용에서 TARGET_NOT_INTERACTABLE로 종료됐습니다. 실패 관측에서 서버 레지스트리는 `patient_a_use_plaster_on_intubation`을 visible:true·canInteract:true로 보고했고, 플레이어와 환자 중심 거리는 약 1.81m였습니다. 기존 `[-1,0,1]`, 반경 0.8 접근이 로컬 근접 스캔 밖에서 끝난 것이 원인이므로, 치료실의 개방된 측면 `[-0.5,0,0.5]`, 반경 0.2로 접근하고 실제 상호작용 노출을 확인하도록 수정했습니다. 임의 이동이나 벽 관통 복구는 추가하지 않았습니다. 모든 프로세스는 EXITED이고 최근 제어 실패와 종료 정리 오류는 없습니다. 수정한 플라스터 적용 경로의 재실기는 남아 있습니다.

근접 접근 재실기 `7c5f1163-efbd-4497-bab0-7abe1ed89966`은 플라스터 상호작용을 실제로 노출·선택했지만 게임이 `(지금은 플라스터를 사용할 수 없다.)`라고 거부했습니다. Quest_Intubation_PatientA는 4/5이고 플라스터도 인벤토리에 남아 있었습니다. 소스와 로그를 대조한 결과 nurse_b 클라이언트에서 `sig.pass_et_tube_ready`와 `sig.remove_intu_stylet`은 정상 등록되지만, TriageScenarioEventBootstrap의 신호 핸들러가 서버 전용 `SetTreatmentApplied`를 클라이언트에서 직접 호출해 `endotracheal_tube_insert_done` 권위 상태가 기록되지 않는 것이 원인입니다. 환자 네트워크 객체에 신호 전용 ServerRpc 경로를 추가했으며, 서버에서 발신자의 nurse_b 역할과 환자 근접 여부를 검증하고 허용된 두 신호만 기존 처치 상태에 반영합니다. TypeScript 타입 검사와 diff 공백 검사는 통과했습니다. C# 변경을 포함한 새 자동화 빌드와 5/5 재실기는 남아 있습니다.

새 빌드 실기 `a7d191e5-1c35-4c49-b5fa-e8dc99b674a2`에서도 플라스터 사용이 거부됐지만, 호스트 GameLogs에는 `endotracheal_tube_stylet_inserted=true`, 이어서 false, `endotracheal_tube_insert_done=true`가 서버 권위 상태로 정상 기록됐습니다. 따라서 신호 RPC는 정상 도착했고 첫 원인 수정은 유효합니다. 남은 원인은 `SetTreatmentApplied`가 B/C 환자만 `SetTreatmentDisplayNetworked`로 전송하고 환자 A의 EndotrachealTubeInsertDone 표시는 서버 로컬에만 적용한 분기였습니다. 플라스터의 정상 적용 판정이 이 표시를 확인하므로 클라이언트에서는 계속 거부됐습니다. 서버 실행에서는 환자 종류와 관계없이 기존 네트워크 표시 전송을 사용하도록 수정했습니다. 새 빌드와 재실기는 남아 있습니다.

환자 A 처치 표시 복제 수정 빌드는 `/private/tmp/ktas-e2e-build-2.log`의 `E2E build completed`와 종료 코드 0으로 생성했습니다. 첫 실기에서는 수정 대상 전의 `LARYNGOSCOPE_HANDOFF_NOT_VISIBLE`, 재실기 `b12dde60-3686-4a0f-93bc-c4107520b726`에서는 기존 의사 접근 복구 중 NAVIGATION_STUCK이 발생해 표시 복제 수정 효과를 아직 판정할 수 없습니다. 후자의 좌표는 `[-71.05,-18.81]`이며, 기존 복구가 중앙 구조물을 z=-18.8에서 곧바로 횡단하려 했습니다. `positions.md`의 이동 그래프에 따라 L3 `[-68,-20.95]`에 합류한 뒤 L4 `[-68,-15.5]`, L5 `[-68,-9.65]`, A_DOOR `[-66.6,-9.65]`, 처치실 내부 `[-61.5,-10]`로 이동하도록 수정했습니다. 모든 프로세스는 EXITED이고 종료 정리 오류와 최근 제어 실패는 없습니다. 새 복구 경로와 기관삽관 5/5 재실기는 남아 있습니다.

복도 경로와 환자 A 표시 복제를 함께 적용한 실기 `73a0e684-afea-4e05-82a1-7c02d5547485`는 기존 전 구간을 재현하고 Quest_Intubation_PatientA를 5/5 completed:true로 완료했습니다. `patient-a-intubation-completed.json`에서 후두경 전달, 준비된 기관내관 전달, 스타일렛 제거, 5cc 주사기 전달, 기관내관 플라스터 고정의 다섯 작업이 모두 1/1·Completed:true임을 확인했습니다. `patient-a-full-route-checkpoint.json`은 `intubation_completed`를 기록하고 전체 경로 미구현을 뜻하는 의도한 오류로 종료했습니다. 네 프로세스는 모두 EXITED이고 cleanupError는 없습니다. 이 결과로 환자 A 처치 표시 복제 수정과 문서 기반 의사 접근 복구 경로는 실기 검증됐습니다. 다음 산소·출혈·IV 병렬 처치부터는 아직 배치 스크립트가 구현되지 않았습니다.

### 2026-09-10: patient_a_critical 실제 4인 전체 경로

최종 macOS 자동화 빌드에서 `node test/live-patient-a-critical.ts config.json patient_a_critical full mac_direct`를 실행한 프로세스 실행 `9104f618-f896-4358-bfb0-b8bb36f93917`이 종료 코드 0으로 통과했습니다. 네 플레이어는 일반 채팅 입력으로 각각 `/a`, `/b`, `/c`, `/d`를 제출해 `nurse_a`, `nurse_b`, `nurse_c`, `nurse_d` 태그를 유지했습니다. 초기 사정, 기도·산소·출혈·정맥로·중심정맥관·Level 1 처치, 맥박 확인, CPR 1차와 2차, ROSC 맥박 확인, 환자 분류 구역 복귀·의복 제거·GCS 확인을 실제 아이템 획득·조합·상호작용 경로로 완료했습니다. 서로 독립적인 역할 행동은 병렬로 실행했습니다.

`patient-a-full-route-completed.json`에서 네 클라이언트가 모두 종단 노드 `E038`에 도달했고 `recoveryNotes`는 모두 빈 배열이며 `fullPlayPassed`는 true입니다. `quest-evidence-export.json`의 네 플레이어 증거 수집도 모두 fulfilled이고 단계별 완료 증거는 같은 실행 폴더의 `patient-a-*-completed.json`에 보존했습니다. 월드 이동은 갱신된 `documentation/positions.md`의 접근 가능 구역과 `check_accessible.py`가 반환한 경유점을 사용하며, 막힌 상태에서 무작위 전진하는 경로는 확정 배치에 포함하지 않았습니다.

검증 도중 UI 문서가 플레이어보다 늦게 준비되면 핫바 초기화가 영구히 누락되어 슬롯 키·클릭이 무효가 되는 문제를 확인했습니다. 지연 초기화와 인벤토리 바인딩 보존을 추가하고, E2E 입력에는 동일한 슬롯 선택 의미를 프레임 순서와 무관하게 실행하는 `hotbarSelect`를 추가했습니다. CPR 2차 퀘스트 완료 뒤 남는 역할별 교육 선택지도 정답 맵으로 처리하도록 고정 배치를 갱신했습니다. 임시 진단 로그를 제거한 뒤 새로 빌드한 결과가 위 최종 실행입니다. 그 직전 동일 전체 경로 통과 실행 `f6e998c0-757b-4ede-b433-e67de07771cc`도 보존합니다.

TypeScript 타입 검사와 `node --test test/runner.test.ts` 43개도 통과했습니다. 이 결과는 `patient_a_critical`의 macOS `mac_direct` 실제 4인 경로 1회 최종 통과를 증명하며, 다른 시나리오·플랫폼이나 반복 안정성 검증으로 확대하지 않습니다.
